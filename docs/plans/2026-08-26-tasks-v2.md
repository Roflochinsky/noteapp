# План: v2 — задачник поверх заметок

Спека: `docs/specs/2026-08-26-tasks-v2.md`. ADR: `docs/adr/2026-08-26-tasks-as-files.md`,
`docs/adr/2026-08-26-app-writes-to-repo.md`. Комп (жёсткое ограничение UI-срезов):
`docs/design/2026-08-26-comp-v2.html` + скриншот `docs/design/2026-08-26-comp-v2.png`,
система — `DESIGN.md`. Факты об API — `docs/research/github-sync-api.md`.
Это LLD и журнал исполнения: попытки, вердикты ревью, ретро — сюда.

Апрув владельца получен сообщением 2026-08-26 («делай всё через /epic, все вопросы решены,
задачи сделаны к утру») — он же подпись под спекой и обоими ADR. Автономный прогон разрешён.
Нарезка ниже — **вторая редакция**, после ревью замысла (вердикты в конце файла).

## Граница автономии

- **Телефон агент не трогает.** Установка APK и прогон на OnePlus 13 — владельцу утром.
  Демонстрация срезов = зелёный полный гейт + APK + сквозной смоук против тестового репо.
- **Боевой репо заметок `voice-notes` и его workflow агент не трогает.** Все смоуки — против
  **тестового** `Roflochinsky/voice-notes-test` (создан 2026-08-26, приватный, фикстуры:
  `projects.md`, `people.md`, четыре задачи в `tasks/`, две заметки, включая немигрированный
  чекбокс). Боевой workflow меняется только после утреннего подтверждения владельцем.
- **Фикстуры реальных ответов API** сняты с тестового репо в
  `app/src/test/resources/github/`: `ref.json`, `trees-recursive.json`, `compare-ahead.json`
  (в последнем есть настоящий `renamed` с `previous_filename`). Сочинять JSON запрещено.
- **Утренняя пачка HITL** (порядок): сначала незакрытый риск v1 — прогон часовой записи
  (`nikitatrubaev-4wi.9`), он про «нажал — записалось» и важнее задачника; потом приёмка
  срезов v2. Любая правка `PipelineQueue`/`RecordingService` в v2 обнуляет этот прогон.

## Модульная раскладка (LLD)

Чистое (JVM-юниты, без Android и сети) отдельно от адаптеров:

```
pipeline/
  TaskFile.kt      чистый  разбор/сборка задачи, подзадачи, имя файла (слаг+коллизии)
  NoteFile.kt      чистый  разбор/сборка заметки; DoneNoteParser — тонкая обёртка над ним
  Registry.kt      чистый  projects.md / people.md
  Edit.kt          чистый  sealed Edit + apply(text, Edit) — смысловая единица правки
  ConflictRule.kt  чистый  трёхсторонний resolve(base, mine, theirs)
  RepoDelta.kt     чистый  compare → действия над кэшем (в т.ч. renamed и «нужен пересбор»)
  BatchPlan.kt     чистый  changes → payload'ы blobs/tree/commit
  Migration.kt     чистый  plan(notes) → List<Change>
  NoteRef.kt       чистый  тождество «локальная запись ↔ путь в репо»
  GithubApi.kt     порт    readRef/readTree/readBlob/readFile/compare/put/delete/commitBatch
  GithubClient.kt  адаптер боевая реализация порта (java.util.Base64, кодирование пути)
  RepoCache.kt     адаптер files/repo/: тексты + карта путь→sha + lastCommitSha + pending
  WriteQueue.kt    адаптер files/repo/queue/: журнал операций, drain-воркер
  RepoStore.kt     фасад   ЕДИНСТВЕННАЯ точка для UI: списки, правки, удаление, статус синка
ui/
  TaskFilter.kt    чистый  фильтрация, сортировка, счётчики для чипов (today параметром)
  TasksScreen.kt · TaskDetailScreen.kt · FilterChips.kt · DeleteDialog.kt — по компу
  FeedScreen.kt · DetailScreen.kt — расширяются
```

### Решения LLD (не переоткрывать в исполнении)

1. **Единица записи — смысловая правка, не текст файла.** `sealed interface Edit`
   (SetField, SetSummary, ToggleSubtask, AddSubtask, Create, Delete) + чистый
   `apply(text, Edit)`. Очередь хранит `Edit`, а не готовый текст: это даёт и переигрывание на
   409, и склейку операций, и трёхсторонний конфликт.
   **Уточнение 2026-08-29 (фикс-цикл Н2, блокер Б2):** `baseSha` в операции нет. Базой
   трёхстороннего слияния служит запись кэша по этому пути, и это не оговорка, а инвариант:
   `refresh()` намеренно не перечитывает пути с ожидающей правкой, а `accept()` кладёт в кэш
   ровно тот текст, который ушёл коммитом. То есть кэш всегда держит то, на чём владелец правил.
   `baseSha` дал бы то же самое только ценой лишнего запроса `git/blobs/:sha` на каждый 409 —
   он не хранит текст, а хранить его дважды незачем. Поле было мёртвым: писалось в журнал,
   читалось обратно и не использовалось нигде, кроме теста, — теперь его нет.
2. **`ConflictRule.resolve(base, mine, theirs): Merged | Divergence(fields)`** — вход
   разобранными структурами. Подзадачи сопоставляются по нормализованному тексту, не по
   позиции; саммари — одно неделимое поле; frontmatter — по ключам.
   **Уточнение 2026-08-29 (фикс-цикл Н2, блокер Б3):** `Merged` — вердикт без данных. Слитый
   файл собирает не правило, а переигрывание той же `Edit` поверх свежего текста из git: так
   тело файла не пересобирается и неизвестные ключи целы (решение LLD-9), а сборка задачи из
   разобранной структуры была бы вторым, расходящимся способом получить тот же файл. Она и
   была мёртвой: `RepoStore` брал из `Merged` только «да/нет».
3. **Порт `GithubApi` + фейк в тестах.** `SyncEngine`/`RepoStore` — классы с внедрёнными
   зависимостями и `today`, не object. Предел повторов 409 — 3, дальше `Divergence`.
4. **Кэш обновляется из ответа записи**: `content.sha` → карта, `commit.sha` →
   `lastCommitSha`. Отдельный опрос ref после записи не нужен.
5. **pending-overlay в кэше**: UI читает кэш+overlay (галочка не отскакивает, офлайн видна);
   входящая дельта пропускает пути с pending либо гонит их через `ConflictRule`.
   **Уточнение 2026-08-29 (фикс-цикл Н2, круг 3, блокер Б1): `RepoStore` — один на процесс.**
   Воркер WorkManager живёт в том же процессе, что и экран (`android:process` в манифесте нет),
   и второй экземпляр над тем же файлом кэша был не требованием архитектуры, а самодельной
   гонкой: `refresh()` и `push()` оба «прочитать снимок → записать снимок», оба сетевые, и
   выигрывал тот, чей `save()` лёг вторым. Экземпляр раздаёт `RepoStore.shared(cacheDir, repo,
   token, api)` — ключ тот же, что был у `MainActivity`. Внутри процесса корутины всё равно
   переплетаются, поэтому `refresh()` и `push()` — единственные, кто кэш переписывает, — взяты
   под обычный монитор (`@Synchronized`), а `RepoCache.snapshot()`/`save()` синхронизированы
   ради видимости `memo`/`seen` между потоками. Файловых блокировок нет и не нужно: писатель
   один. Цена — pull-to-refresh ждёт один сетевой запрос отправки (паузу в секунду между
   мутациями воркер держит между вызовами `push()`, то есть вне замка).
6. **Очередь**: файловый журнал `files/repo/queue/` (по файлу на операцию: тип, путь, Edit,
   baseSha, id, попытка); один drain-воркер в уникальной цепочке `repo-write`
   (`APPEND_OR_REPLACE`), внутри — цикл с `delay(1000)` между мутациями; `failure()` только
   на нашем баге, сеть/409/лимит — `retry()`. **Уточнение 2026-08-29 (фикс-цикл Н2, Н-13):**
   третий случай — токена нет в настройках. Это не сеть и не наш баг, `retry()` крутил бы
   цепочку вечно: воркер отдаёт `failure()`. Правки при этом не теряются — они лежат в
   файловом журнале, а воркер заводится заново из `MainActivity.store()`, как только токен
   появился. **Пуш заметок v1 — своя цепочка и приоритет:
   правки задач никогда не задерживают доставку свежей записи** (принцип 2).
7. **`inbox/` для приложения — только чтение.** Правки и удаление — лишь по обработанным
   заметкам и задачам; иначе гонка с Action.
8. **Запись только со свежим sha.** PUT без `sha` разрешён только при создании нового файла;
   404/422 = путь уехал → перечитать карту, файл не создавать (иначе воскресший дубль).
9. **Инвариант формата — идемпотентность канонизации**, а не побайтовый круговой прогон:
   `build(parse(build(parse(x)))) == build(parse(x))`, парсер принимает неканоничный ввод
   (лишние пробелы, `- [X]`, переставленные ключи, кавычки, кириллица). **Неизвестные ключи
   frontmatter сохраняются.** Правка frontmatter — построчная, тело файла не переписывается.
10. **`NoteRef`** заменяет `findDonePath`: тождество «локальная запись ↔ путь в репо» живёт в
    кэше и переживает переименование Action-ом (`renamed`/`previous_filename`, плюс запасное
    тождество по `recorded` из frontmatter, если `compare` отдал `removed`+`added`).
11. **Лента = локальные записи `NotesStore` ∪ заметки из кэша**, ключ склейки — `NoteRef`.
    Запись без сети видна в ленте со статусом и не двоится после пуша (принцип 4).
12. **UI никогда не ждёт сеть**: приложение открывается на вкладке «Заметки», лента и кнопка
    «Записать» рисуются из кэша мгновенно, синк идёт фоном; расхождение по 409 показывается
    непрерывающей плашкой в деталке, не модалкой.
13. **`RepoCache(dir)`**: json с `version` и `repo`, запись через temp+rename; битый файл,
    чужая версия или смена репо/токена → холодный старт, не исключение.
14. **Кодирование пути и Base64** — по одному месту на всё (`java.util.Base64`, minSdk 34);
    существующий `GithubClient.putFile` переводится на них же.
15. **Имя файла задачи** — `TaskFile.fileName(date, title, taken)`: транслит кириллицы,
    kebab-case, при коллизии суффикс `-2`. Чужой файл молча не перезаписывается.
16. **Сортировка задач**: приоритет P1→P3, внутри — просроченные, затем по сроку, затем без
    срока по дате создания. Сделанные — в свёрнутой секции.
17. **Состояние фильтров** — `rememberSaveable`, между запусками не живёт.
26. **Свободного ввода нового проекта в деталке задачи нет до среза Н3** (решение
    2026-08-29, фикс-цикл Н2, находка Д7). Комп обещает «новый проект запишется в
    `projects.md`», но реестра до Н3 не существует: ввод рождал бы проект, которого нет ни в
    одном реестре и ни в одной другой задаче, — ровно та «метка без реестра», против которой
    и заведено решение 3 спеки. До Н3 проект выбирается из значений, которые уже встречаются
    в задачах, плюс «Без проекта». Шторка создания задачи вела себя так с самого начала —
    теперь деталка ей не противоречит.
27. **Диалог удаления задачи — без галочек** (решение 2026-08-29, фикс-цикл Н2, находка Д12).
    Спека (пункт 9) и план требуют «диалог с галочками» и «галочка по умолчанию», но три
    галочки спеки — это цели заметки: файл в репо, аудио на телефоне, порождённые задачи. У
    задачи из них применима ровно одна — файл в репо; аудио у задачи нет, задач она не
    порождает. Единственная галочка, всегда отмеченная и ничего не меняющая, — это лишний тап
    перед необратимым действием, а не защита от него. Диалог остаётся тем же по составу
    (заголовок, название, путь файла, «Файл исчезнет из репозитория», «Удалить»/«Отмена») —
    без чекбоксов. Галочки появляются там, где целей действительно больше одной: удаление
    заметки вместе с её задачами, срез Н6. Ссылку на задачу в заметке-источнике удаление пока
    не чистит — отдельное решение LLD-18 и `bd nikitatrubaev-0rk.15`.

### Развилки, которые ревью оставило ведущему (решено 2026-08-26, не переоткрывать)

18. **Удаление задачи убирает ссылку на неё в заметке-источнике** тем же коммитом. Обратный
    индекс не нужен: `source:` у задачи и есть путь заметки. Иначе репо гниёт мёртвыми
    ссылками, а его смысл — читаться глазами в GitHub.
    **Отсрочка 2026-08-29 (фикс-цикл Н2, находка Д4): решение переезжает в срез Н6**, задача
    `bd nikitatrubaev-0rk.15`. Причина не в лени: «тем же коммитом» — это две правки в одном
    коммите, а contents API так не умеет; нужен батч через git trees API, которого в границах
    Н2 нет по определению («батч-коммитов нет», раздел «Н2»). Вторая помеха — текста заметок
    нет в кэше, он держит только `tasks/`. Оба куска и есть предмет Н6 (удаление заметки
    вместе с задачами). До Н6 удаление задачи оставляет в заметке висячую ссылку — это
    известный долг, а не сюрприз.
19. **Удаление заметки не трогает её задачи**, если галочка не отмечена. Их `source`
    становится висячим — приложение показывает «заметка удалена», кнопка источника
    неактивна. Чистить чужие файлы без спроса нельзя.
20. **Правка заголовка заметки файл не переименовывает.** Путь заметки неизменен после
    переноса Action-ом — иначе рвутся `source` у задач, кэш и очередь.
21. **`zipball` выкинут из скоупа.** Холодный старт — `trees?recursive=1` + чтение файлов;
    при усечении `compare` или `diverged` — свежая карта через `trees` и дочитывание **только
    файлов с изменившимся blobSha**. Причина: 302 на подписанный URL с переносом заголовка
    `Authorization` — лишняя ловушка ради одного запроса. Спека приведена в соответствие.
22. **Папки типов — первого уровня**, вложенности нет: на этом стоит относительная ссылка
    `../tasks/…` из любой папки типа.
23. **Кэш хранит тексты целиком** (личный репо — сотни файлов), сбрасывается при смене репо
    или токена. Граница пересматривается, если репо перевалит за пару тысяч файлов.
24. **Одиночный PUT — внутренняя оптимизация `RepoStore`, а не второй публичный путь.**
    Экраны не выбирают между contents API и батчем и вообще не знают про GitHub.
25. **Эталонная пара «промпт ↔ парсер»** живёт в `app/src/test/resources/action/`: заметка в
    том виде, как её оставляет Action, и порождённые задачи. Промпт меняется только вместе с
    этой фикстурой — иначе слои разъедутся молча.

## Срезы

Нарезка вертикальная: после каждого владелец видит поведение, а не отчёт о префакторе.

### Н1 — задачи видны списком *(демо: положить файл в `tasks/` тестового репо → открыть приложение → он в списке)*

`TaskFile.parse` + `NoteFile.parse/build` + `GithubApi`/фейк + `GithubClient` (readTree,
readBlob, readFile) + `RepoCache` + `RepoStore` (только чтение) + `TasksScreen` с рубриками
P1–P3, свёрнутым «Сделано» и переключателем вкладок. Полное чтение `tasks/` (без ETag и
compare — репо личный). Тесты: `TaskFile` на неканоничных фикстурах, `TaskFilter` сортировка
с `today` параметром, `RepoCache` на `TemporaryFolder` (битый json → холодный старт),
`RepoStore` на фейке порта. Смоук: чтение живого `voice-notes-test`.

### Н2 — пульт заработал *(демо: тап по чекбоксу → коммит в тестовом репо; правка полей; создание; удаление задачи; всё это офлайн — уходит при появлении сети)*

`Edit` + `apply` + `WriteQueue` + обновление кэша из ответа + pending-overlay +
`ConflictRule` + `TaskDetailScreen` (сегмент статуса, поля, подзадачи, ссылка на источник) +
создание шторкой + удаление одной задачи (contents `DELETE`, галочка по умолчанию).
Тесты: два тапа → одна операция; дельта не трогает pending; конфликт «мы приоритет, они
срок» → Merged; «оба статус» → Divergence; предел повторов; переживание убитого процесса.
Смоук: полный цикл против тестового репо, проверка `gh api` что файл изменился.

### Н3 — список наполнился *(демо: сухой прогон показывает, что мигрирует; прогон — одним коммитом; фильтры сужают список)*

`Migration.plan` + `BatchPlan` + `commitBatch` + `Registry` + `FilterChips` (проект,
приоритет, статус) со значениями из реестров и счётчиками. Миграция — **один** коммит с
сообщением `migration: tasks v2` (откат одним `revert`), идемпотентность по состоянию
заметки (чекбокс, ставший ссылкой, повторно не мигрирует), имя файла задачи детерминировано.
Тесты: `Migration.plan` на фикстуре с немигрированным чекбоксом (в тестовом репо такая
заметка есть), повторный прогон даёт пустой план; `BatchPlan` (удаление = `sha: null`,
переименование = удаление+создание). Смоук: сухой прогон и реальный на тестовом репо.

### Н4 — Action v2: задачи из разговора *(демо: прогон workflow на тестовом репо)*

Промпт в `docs/examples/process-notes.yml`: извлекать задачи в `tasks/` **после** переноса
заметки (чтобы `source:` указывал на конечный путь), ссылки вместо чекбоксов, сопоставление
с `people.md` без пополнения реестра, уникальность имён файлов задач, существующие файлы в
`tasks/` не изменять. В workflow — `pull --rebase` + повтор перед push (иначе коммит
приложения роняет job). Боевой workflow — только после утреннего подтверждения.

Критерии приёмки среза:

1. Заметка с задачами в разговоре → в `tasks/` новые файлы, в теле заметки на месте
   чекбоксов ссылки `- [Заголовок](../tasks/…md)`, чекбоксов-дублей не осталось.
2. **Ни один `source:` не указывает на `inbox/`** — только на конечный путь заметки в папке
   типа. Это ровно то, что делает ссылку «задача → заметка-источник» рабочей. Инвариант,
   на котором держится относительная ссылка `../tasks/…`: папки типов — первого уровня,
   вложенных папок в репо заметок нет.
3. **Обратная совместимость: в разговоре нет задач → заметка обрабатывается как v1** —
   секции «Задачи» нет, остальное без изменений, кроме разделения блоков саммари пустой
   строкой (без неё GitHub слепляет их в абзац; так же написана фикстура в тестовом репо).
4. Файлы, лежавшие в `tasks/` до прогона, не изменены и не перезаписаны; имена новых
   уникальны (при совпадении даты и слага — суффикс `-2`, `-3`).
5. Новых записей в `people.md` и `projects.md` не появилось; незнакомое имя осталось в
   тексте как услышано.
6. `actionlint docs/examples/process-notes.yml` зелёный.
7. Эталонная пара фикстур `app/src/test/resources/action/` сверена вручную с входом в
   тестовом репо: транскрипты совпадают с `inbox/`-исходниками побайтово, каждое поле меты
   выводится из текста разговора. После первого живого прогона она сверяется с его
   результатом **по содержимому, а не по имени файла**: имя
   `tasks/2026-08-26-podnyat-limit-retraev.md` в тестовом репо занято нарочно (blob
   `7b9f54a6…`, запись журнала 2026-08-26 03:1x), поэтому прогон обязан оставить тот файл
   нетронутым и создать `2026-08-26-podnyat-limit-retraev-2.md`; с тем же суффиксом придёт
   и ссылка на него в заметке. Расхождение ровно в этом суффиксе — пройденный критерий 4,
   а не провал сверки (правило «промпт меняется только вместе с фикстурой» — README там же).

### Н5 — лента заметок v2 *(демо: фильтры сужают ленту; правка полей заметки видна в GitHub)*

Склейка `NotesStore ∪ RepoCache` по `NoteRef`, чипы ленты (тип/проект/персона/тег/дата),
деталка заметки: поля-чипы, выбор участников из реестра, задачи ссылками. Правка **текста
саммари** — последний необязательный пункт среза, делать только если остальное зелёное.

**Долг Н1, закрываемый здесь:** `DetailScreen` создаёт `GithubClient` сам и зовёт
`findDonePath`/`readFile` мимо `RepoStore` — инвариант «UI не ходит в API напрямую» нарушен со
времён v1. Срез обязан снять это вместе со склейкой: чтение заметки идёт через фасад, прямых
вызовов клиента в `ui/` не остаётся (в коде помечено `// ponytail:`).

### Н6 — удаление заметки вместе с задачами *(демо: диалог с галочками → один коммит)*

`DeleteDialog` (по умолчанию только файл в репо), «задачи этой заметки» определяются
сканированием `source:` у задач в кэше (не парсингом ссылок в тексте), удаление аудио —
второе подтверждение и запрет для заметки, ещё не ушедшей в репо.

### Н7 — обновление не тратит лимит *(демо: два refresh подряд — второй не тратит квоту)*

ETag на ref → `compare` → `RepoDelta` → дочитывать только файлы с изменившимся blobSha;
усечённый `compare`/`diverged` → пересбор `trees` + чтение изменённых. Ручной pull-to-refresh
— всегда безусловный запрос (поведение ETag/CDN не проверено). Закрывает критерий 14.

### Н8 — за чертой утра (не ночью)

Переименование персоны по всем заметкам одним коммитом; ручное пополнение реестра персон из
предложений Claude. Дорогой и самый рискованный жест — только после приёмки.

**Порядок:** Н1 → Н2 → Н3 (ядро запроса владельца) → Н4 → Н5 → Н6 → Н7. Параллель возможна
только Н4 с Н5+ (разные слои); ночью ведём последовательно — общее дерево, один пишущий.

## Вердикты ревью замысла (2026-08-26)

Четыре оси параллельными агентами. Все находки внесены в решения LLD и нарезку выше.

- **Бизнес — CHANGES REQUESTED** (13). Ключевое: первые три среза не давали владельцу ничего
  осязаемого → вертикальная перенарезка Н1–Н3; миграция поднята рано (иначе экран задач утром
  пуст); лента не должна терять локальные записи (принцип 4); правки задач не должны
  задерживать пуш записи; удаление одной задачи вынуто из тяжёлого батч-среза; персоны и
  правка саммари — за черту; Action не второй и требует критерия обратной совместимости;
  миграция — одним коммитом с откатом через revert; нужен тестовый репо (создан).
- **LLD — CHANGES REQUESTED** (13 + мелочи). Ключевое: журнал очереди в файлах и один
  drain-воркер (WorkManager Data 10 КБ, KEEP глотает вторую операцию, APPEND рушит цепочку);
  кэш обновляется из ответа записи; pending-overlay; трёхсторонний `ConflictRule` на
  структурах; порт `GithubApi` и фейк вместо моков HTTP; `BatchPlan` отдельно от сети;
  `java.util.Base64`; идемпотентность канонизации вместо побайтового прогона; `NoteFile`
  в первый срез; реальные фикстуры API (сняты); контракт `RepoCache`; перенарезка С3/С5/С7/С8.
- **HLD — CHANGES REQUESTED** (12 + мелочи). Ключевое: единица записи — `Edit`, не текст;
  `NoteRef` вместо `findDonePath`; `inbox/` только для чтения и `pull --rebase` в workflow;
  `source:` пишется после переноса заметки; `renamed` может прийти как `removed`+`added`;
  усечённый compare — дочитывать только изменившиеся blob'ы; схема формата в одном месте и
  эталонная пара фикстур; противоречие «Action создаёт задачи / не трогает tasks/» снято
  формулировкой «создаёт при обработке raw, существующие не изменяет»; единый фасад
  `RepoStore` для UI; `HttpTransport`/порт вместо непроверяемого object.
- **UX — CHANGES REQUESTED** (19). Ключевое и блокирующее: (1) комп обещает «Сделано за
  месяц · N», а даты закрытия в формате не было → в ADR добавлено поле `done` (правило
  показа — 30 дней от сегодня; `done`-задачи без поля идут в конец секции без даты);
  (2) в `DocPalette` нет токена `Err`, а `error` указывает на коралл — деструктивные контролы
  выедут коралловыми; чинить там, где появится первая кнопка «Удалить» (Н2).
  Дописано в контракты срезов: пустые состояния (нет задач / пусто под фильтром) и правило
  «нулевые значения в шторках остаются со счётчиком 0»; одна поверхность ошибок синка строкой
  под шапкой (`mut`/`amber`, `err` запрещён, `Toast` запрещён); три состояния записи
  (оптимистично → «в очереди» янтарём → снекбар «победил git»); панель записи важнее «Новой
  задачи» на обеих вкладках; семантика `in_progress` и чекбокса; 48dp-таргеты и отмена
  снекбаром 5 с до отправки; скролл-контракт деталки задачи (шапка и `statusline` вне
  скролла, теги `FlowRow`); карандаш в шапке = правка заголовка на месте; чипы `LazyRow` с
  фасетными счётчиками (свой фильтр исключён из расчёта); правка саммари — сырой markdown
  байтовым диапазоном, хвост файла не пересобирается; карандаш персон рисуется в Н5, но
  включается только в Н8; диалог удаления строит цели динамически (нулевые строки не рисуются,
  одна цель → без чекбоксов); Back/вкладка в состоянии экрана, `imePadding` в шторках ввода,
  репо-кэш вне секундного поллинга; лупа в шапке не рисуется до решения владельца; очередь в
  ленте — янтарём (сейчас синим), `PullToRefreshBox` — единственный спиннер, роли типографики
  добавляются в тему один раз.
  **Оба вопроса владельцу закрыты 2026-08-26 (решение владельца):** (а) **поиск нужен** —
  подстрочный поиск по загруженному кэшу (заголовок и превью заметки, заголовок задачи),
  лупа в шапке на обеих вкладках, отдельного экрана нет: тап разворачивает поле ввода в
  шапке, результат сужает текущий список поверх активных фильтров. Живёт в `TaskFilter`
  (задачи) и `FeedItem`-фильтре (лента), реализуется в срезах Н3 и Н5; в Н1 лупа не
  нарисована — довешивается в Н3 тем же кодом, что и чипы; (б) **подтверждено**: из деталки
  заметки v2 «Поделиться» уходит, «Повторить отправку» переезжает третьим состоянием
  статус-строки (`!pushed` → янтарная строка «не отправлено · Повторить»).

## Журнал исполнения

- 2026-08-26 02:2x — создан тестовый репо `voice-notes-test` с фикстурами; сняты реальные
  ответы API (`ref`, `trees?recursive=1`, `compare` с `renamed`) в
  `app/src/test/resources/github/`.
- 2026-08-26 03:0x — Н4 сдан (ветка `worktree-agent-a515599baf0b301dd`, коммит `da5e53f`),
  гейты зелёные. Попутная находка исполнителя: гейт `actionlint` в репо не срабатывал ни разу
  (смотрел в несуществующий `.github/workflows/`) — область хука и таблица в CLAUDE.md
  исправлены.
- 2026-08-26 03:0x — фикстуры тестового репо: транскрипт подписан «Спикер N» (был именами —
  расходилось с форматом v1).
- 2026-08-26 03:1x — ревью Н4: **CHANGES REQUESTED**, 7 находок. Заявление автора про отзыв
  токена `claude-code-action` ревьюер проверил по логу прогона и подтвердил (шаг пуша
  обоснован). Блокер: эталонные фикстуры переписывали транскрипт, из-за чего `project`,
  `priority` и `participants` в них не выводятся из разговора — контракт был бы фиктивным.
  Отправлено в фикс-цикл (попытка 1 по срезу `nikitatrubaev-0rk.4`).
- 2026-08-26 03:0x — холостой диспатч workflow на тестовом репо
  ([run 32911976137](https://github.com/Roflochinsky/voice-notes-test/actions/runs/32911976137))
  подтвердил: relay корректно пропускается на `workflow_dispatch`, `process` доходит до шага
  action и падает на отсутствии `CLAUDE_CODE_OAUTH_TOKEN` — секрет создаёт владелец. Там же в
  логе видно, как `claude-code-action` сносит заголовок авторизации от checkout и отзывает свой
  app-токен в конце шага — доказательство под ADR-дополнением 2026-08-26.
- 2026-08-26 03:1x — в тестовый репо положен файл `tasks/2026-08-26-podnyat-limit-retraev.md`,
  занимающий имя будущей задачи: утренний прогон обязан оставить его нетронутым
  (blob `7b9f54a6bb2b1530ef8825f43af8bb3c82d5e6e9`) и создать новую задачу с суффиксом `-2`.
- 2026-08-26 03:0x — **Н1 сделан** (ветка `worktree-agent-a26ff3c50a9437b46`): `Frontmatter`,
  `TaskFile`, `NoteFile` (+ `DoneNoteParser` стал обёрткой), порт `GithubApi` и фейк,
  `GithubClient` классом-адаптером на `java.util.Base64` с единым кодированием пути, `RepoCache`,
  `RepoStore`, `TaskFilter`, `TasksScreen` + `SectionTabs`/`SyncLine`, вкладки в `MainActivity`.
  Гейт зелёный (50 тестов, 1 — смоук — пропускается без токена), APK собран; смоук против
  `voice-notes-test` читает 6 задач в правильном порядке. Лупы в шапке нет по решению плана
  2026-08-26(а): поиск нужен, но приходит в Н3 вместе с чипами. «Новая задача» — неактивная
  заглушка до Н2.

- 2026-08-29 — ревью Н1: **CHANGES REQUESTED**, 2 блокера + 9 некритичных. Мутационная
  проверка честная (две мутации — `TaskFile` умолчание приоритета, ключ сортировки
  `TaskFilter` — убили ровно свой тест, sha до/после сверены). Блокеры: (Б1) `RepoCache`
  сверяет только `version`/`repo`, токена в json нет — при смене токена старый снимок
  остаётся на экране, нарушены LLD-13 и LLD-23; (Б2) реальные фикстуры
  `app/src/test/resources/github/` не читает ни один тест, разбор JSON в `GithubClient`
  проверяет только пропускаемый смоук — в гейте покрытие адаптера нулевое.
  В фикс-цикл взяты Б1, Б2 и Н-1 (пустое состояние врёт при всех сделанных), Н-2 (строка
  ошибок синка есть только на вкладке «Задачи»), Н-3 (тач-таргет строки синка 34dp вместо 48),
  Н-4 (разделитель после последней строки рубрики — расхождение с компом), Н-5 (`Frontmatter.
  render` теряет неизвестный ключ с пустым значением, LLD-9), Н-6 (`String(ByteArray)` без
  `Charsets.UTF_8` — кириллица зависит от локали хоста). Попытка 1 по срезу
  `nikitatrubaev-0rk.1`.
  Отдано в Н2 долгом: Н-7 (`findDonePath` зовёт `readTree("main")`, хотя порт объявлен по SHA
  коммита), Н-8 (`Settings` читаются на главном потоке в `MainActivity.store()`). Н-9
  (`TaskFile.fileName` без вызывающего кода до Н2) — осознанное решение, не чиним.
  Живой смоук против `voice-notes-test` в этот прогон не гонялся: сеть до github.com из WSL
  висит (`git ls-remote` и авторизованный `api.github.com` — таймаут, неавторизованный
  отвечает за 0.26 с). Офлайновый гейт зелёный: 52 теста, 1 пропуск, APK собран.

- 2026-08-29 — **Н1, фикс-цикл (попытка 1 закрыта)**. Блокеры: (Б1) личность кэша уехала в
  конструктор — `RepoCache(dir, repo, token)` c `load()`/`save(snapshot)`, в json пишется
  `tokenHash` (SHA-256 отпечаток, сам токен на диск не попадает); снимок из-под другого или
  снятого токена не отдаётся. `RepoStore` при этом потерял параметр `repo` — он был нужен
  только кэшу, теперь подпись `RepoStore(cache, api)`. (Б2) у `GithubClient` появился шов
  транспорта — третий параметр `fetch: (String) -> String` с боевым `httpGet` по умолчанию;
  новый `GithubClientTest` разбирает снятые с репо `ref.json` и `trees-recursive.json`
  офлайн и проверяет процентную кодировку кириллического пути. Некритичные: Н-1
  (`TaskFilter.nothingToShow` — пусто только когда нет ни открытых, ни свежесделанных),
  Н-2 (`SyncLine` на обеих вкладках — первый синк уходит на «Заметках»), Н-3 (тач-таргет
  строки синка 48dp), Н-4 (`itemsIndexed` + линия только между соседями, по `.trow + .trow`),
  Н-5 (`Frontmatter.render` пишет ключ без значения — неизвестные ключи переживают
  пересборку). Долг документации закрыт: расхождение «срок моно вопреки правилу моно-цифр»
  названо в `DESIGN.md`.
  **Н-6 оказался ложной тревогой:** Kotlin `String(ByteArray)` компилируется в
  `new String(bytes, Charsets.UTF_8)` (проверено `javap -c` по классу `GithubClient`), от
  `file.encoding` разбор не зависел никогда. Явный `Charsets.UTF_8` всё равно проставлен —
  как подпись намерения, падающего теста под него не существует.
  Гейт: 61 тест, 1 пропуск (смоук без токена), APK собран. Мутационная проверка честная:
  четыре мутации (сверка токена, фильтр `type == blob`, отбрасывание пустых значений в
  `render`, пустота без учёта сделанных) убили ровно свои пять тестов и ничего сверх.
  Красное по дороге: (1) `ktfmtCheck` — `GithubClient.kt` и `TaskFilter.kt` не отформатированы
  (лечится `ktfmtFormat`); (2) `detekt TooManyFunctions` — вынесенный `RowDivider()` сделал в
  `TasksScreen.kt` 11 функций при пороге 11; разделитель вписан обратно по месту, порог не
  трогали.
  Не закрыто и остаётся риском: разбор ответа `git/blobs` офлайном по-прежнему не покрыт —
  фикстуры такого ответа нет, а сочинять json запрещено; держит его только пропускаемый
  смоук. `compare-ahead.json` штатно ждёт метода `compare` в срезе Н7. Живой смоук снова не
  гонялся: сеть до github.com из WSL висит.

- 2026-08-29 — фикс-цикл Н1 (коммит `8dc1ee4`) и **повторное ревью: APPROVED**. Б1 закрыт
  личностью кэша в конструкторе (`RepoCache(dir, repo, token)`, в json — SHA-256 отпечаток,
  сам токен на диск не попадает), Б2 — швом транспорта `fetch: (String) -> String` и
  офлайновым `GithubClientTest` на настоящих `ref.json`/`trees-recursive.json`. Тестов 52 → 61.
  Ревьюер сверил дословность переноса боевого HTTP-пути построчно и подтвердил мутациями,
  что новые тесты честные. **Н-6 снята как ложная тревога ревьюера**: `String(ByteArray)` в
  Kotlin — inline-функция стдлиба, жёстко зашивающая UTF-8 (дизассемблирован
  `kotlin/text/StringsKt__StringsJVMKt` из `kotlin-stdlib-2.0.21.jar`, не класс автора);
  явный `Charsets.UTF_8` оставлен подписью намерения.
  Косметика после APPROVED, взята ведущим тем же срезом: правка Н-4 попутно съела хайрлайн
  над «Сделано» (комп `.fold{border-top}`, строка 82) — вернула его; заодно разделители
  задач стали full-bleed, как требуют и комп (`border-top` висит на самой `.trow` с её
  паддингом), и `DESIGN.md`:195 («full-bleed строки, разделённые hairline 1px»).
  **Открытый риск:** боевой `httpGet` в JVM смоуком не прогнан — сеть до github.com из WSL
  работает урывками (1-2 успеха на 10 запросов; `RepoSmokeTest` делает девять подряд и падает
  на первом отказе, шесть попыток — `expected:<OK> but was:<OFFLINE>`). Ревьюер заместил это
  вытягиванием тех же ответов через curl с ретраями: `git/blobs` живьём приходит с переносами
  строк в `content` — значит `getMimeDecoder()` выбран верно; `contents/` с процентной
  кириллицей отдаёт 200; `trees?recursive=1` не усечён, в `tasks/` шесть задач, фикстура
  `2026-08-26-podnyat-limit-retraev.md` цела (blob `7b9f54a6`). Смоук прогнать, когда сеть
  починится. Офлайн не покрыт разбор ответа `git/blobs` — фикстуры нет, сочинять запрещено.

- 2026-08-29 — **Н2 доведён до контрольной точки** (ветка `slice-h2`, коммит `2ca435f`).
  Журнал пишет ведущий: исполнитель умер на ошибке авторизации (403) после шести часов
  работы, ничего сдать не успел, поэтому запись восстановлена по дереву и прогону.
  Состояние на момент смерти: гейт **полностью зелёный, 98 тестов** (на main было 61),
  0 падений, 2 пропуска — оба смоука без токена. Появились `Edit` (9 тестов), `WriteQueue`
  (7), `ConflictRule` (7), `RepoStoreWriteTest` (10), `GithubApiTest` (2); `TaskDetailScreen`
  и `TaskSheets` (`ChoiceSheet`/`InputSheet`/`DueDialog`/`DeleteTaskDialog`/`NewTaskSheet`)
  подключены в `MainActivity`. Ветка дважды вливала main: Н1 после ревью (сменились подписи
  `RepoCache`/`RepoStore`) и правку скилла.
  Три долга, отданные в этот срез ревью Н1, закрыты: Н-7 — `findDonePath` переехал
  расширением над портом и читает дерево по `readRef()`, а не по имени ветки; Н-8 — `store()`
  стал `suspend` и читает `Settings` внутри `withContext(Dispatchers.IO)`; в `DocPalette`
  заведён токен `Err` (нативный красный), `error` больше не указывает на коралл записи.
  **Требует решения владельца:** тронут `config/detekt/detekt.yml` — `TooManyFunctions`
  получил `ignoreAnnotated: ['Composable']` и `ignorePrivate: true`. Обоснование в самом
  конфиге (композабл — разметка, а не метод класса; правило меряет публичную поверхность),
  но это послабление статанализа на весь репозиторий, а такие правки владелец подписывает.
  Сквозной смоук против тестового репо снова не гонялся — сеть до github.com рвётся
  (`nikitatrubaev-0rk.10`).
- 2026-08-29 — **долг Н4 (`nikitatrubaev-0rk.8`) закрыт и слит в main** (`db93b2d`). Ревью:
  APPROVED с шестью некритичными; фикс-цикл закрыл все шесть. Существенная была одна:
  ассерт `assertEquals(TaskFile.PRIORITY_DEFAULT, bare.priority)` сравнивал продакшн-константу
  с продакшн-умолчанием и не отличал «поле есть» от «поля нет» — ревьюер снёс `priority: P2`
  из фикстуры, и все 65 тестов остались зелёными. Теперь сверяется литерал `"P2"` плюс
  наличие ключа в тексте файла; тем же приёмом починен близнец по `status`. Мутация
  подтверждена: удаление `priority: P2` валит `ActionFixtureTest` на строке проверки ключа.
  Хелпер фикстур вынесен в `ActionFixture.kt` (имя файла продиктовал detekt
  `MatchingDeclarationName`), якорь ресурса отцеплен от продакшн-класса `NoteFile`, KDoc
  перестал обещать, что тест ловит расхождение с промптом — он его не читает.
  Тестов на main 61 → 66, гейт зелёный. Побайтовость транскрипта тестом по-прежнему не
  закрыта (raw-половины пары в фикстурах нет) — долг `nikitatrubaev-0rk.12`.

- 2026-08-29 — **ревью Н2: CHANGES REQUESTED**, 3 блокера + 13 некритичных. Гейт зелёный (98
  тестов), границы среза соблюдены, три долга Н1 закрыты по-настоящему — но центральное
  поведение демо ломается:
  **Б1 — галочка отскакивает ровно в момент успешного коммита.** Экранный `RepoStore` и
  `RepoStore` воркера держат каждый свой `snapshot`, прочитанный из кэша один раз в
  конструкторе; экранный `push()` не зовёт никогда. Как только воркер отправил правку и убрал
  операцию из журнала, `overlay()` отдаёт старый текст без правки, а `watchQueue()` раз в
  секунду перерисовывает список — «Сделана» возвращается в «Открыта» до следующего
  pull-to-refresh. Ревьюер подтвердил пробой: два `RepoStore` над одним кэшем дают
  «ГАЛОЧКА ОТСКОЧИЛА: экран показывает open после успешного коммита». 98 тестов этого не
  ловят, потому что `RepoStoreWriteTest` правит и отправляет одним экземпляром. Это ровно
  LLD-5, ради которого задуман pending-overlay.
  **Б2 — `WriteQueue.Op.baseSha` мёртв**: пишется в журнал и читается обратно, но базой
  трёхстороннего слияния служит текущая запись кэша, а держится это на оговорке «`refresh()`
  не перечитывает pending-пути». LLD-1 в этой части не реализован, тест закрепляет поле, а
  не поведение.
  **Б3 — `ConflictRule.Merged.task` мёртвый код**: `RepoStore` использует `Merged` как булев
  флаг, собранную задачу выбрасывает и переигрывает `Edit` заново. Значит `task()`/`body()`/
  `subtasks()` не исполняются в приложении никогда, а пять тестов `ConflictRuleTest`
  проверяют то, чего в поведении нет — мутация в них убьёт тест, не изменив ни байта в репо.
  Мутационная проверка ядра при этом честная: дедупликация двух тапов, порядок ключей по ADR
  и предел переигрываний 409 — каждая мутация убила ровно свой тест.
  **Про `config/detekt/detekt.yml` — фактура для владельца.** Ревьюер вернул конфиг и
  прогнал detekt: без послабления падают шесть находок (`WriteQueue` 11, `RepoStore` 24,
  `MainActivity` 17 функций; файлы `TaskSheets` 12, `TaskDetailScreen` 17, `TasksScreen` 12).
  С одним лишь `ignoreAnnotated: ['Composable']` вывод **байт в байт тот же** — правило вешает
  находку на файл или класс, а они не аннотированы, фильтр по аннотации до функций не доходит.
  То есть обоснование, записанное в конфиге, описывает половину правки, которая не делает
  ничего; глушит всё `ignorePrivate: true` — и глушит в том числе `RepoStore` и
  `MainActivity`, то есть ровно те два класса, где ревью нашло Б1. Прецедент репо обратный:
  в фикс-цикле Н1 такую же находку закрыли кодом, порог не трогая.

- 2026-08-29 — **фикс-цикл Н2: три блокера и тринадцать некритичных закрыты** (ветка
  `slice-h2`). Тестов 98 → **105**, падений 0, пропуска 2 — оба смоука без токена.
  **Б1 — галочка отскакивала**, потому что `RepoStore` читал снимок кэша один раз в конструктор,
  а экземпляров два: экранный (живёт всю сессию, `push()` не зовёт никогда) и воркера записи.
  Лечили причину: снимка-поля больше нет, `RepoStore.snapshot` — чтение через `RepoCache`,
  а кэш перечитывает файл, только если сменилась метка «время + размер». Сначала тест
  (`галочка не отскакивает, когда коммит отправил другой экземпляр`) — на старом коде он падал
  строкой «ГАЛОЧКА ОТСКОЧИЛА». **Мутация подтверждает:** `if (memo != null && now == seen)` →
  `if (memo != null)` (снимок снова читается один раз) даёт ровно `1 failed` и ровно этот тест;
  sha256 файла до `6af30843`, под мутацией `96a77358`, после отката копией — снова `6af30843`.
  **Б2 — `baseSha` выброшен**, база слияния — запись кэша; обоснование записано уточнением
  LLD-1. **Б3 — `ConflictRule.Merged` стал вердиктом без данных**, `task()`/`body()`/`subtasks()`
  (≈60 строк) удалены, уточнение LLD-2; пять ассертов `ConflictRuleTest` про собранную задачу
  заменены: вердикт проверяется здесь, а поведение подзадач — новым тестом
  `RepoStoreWriteTest.на 409 чужое удаление подзадачи не воскрешает её нашей галочкой`, где
  переигрывание правки происходит по-настоящему.
  Некритичные: Н-1 плашка расхождения теперь и в деталке (LLD-12 буквально этого и требовал);
  Н-2 тач-таргеты 48dp — сегмент статуса, чип тега, «+ тег», чипы шторки создания, «ОТМЕНИТЬ»
  в снекбаре (приём прежний: прозрачная полоса вокруг видимого элемента, чтобы комп не
  поехал); Н-3 вернулась подпись «задача станет файлом в tasks/ и уйдёт коммитом»; Н-5 уход с
  деталки исчезнувшей задачи — `LaunchedEffect`, а не запись в state во время композиции;
  Н-6 секундный тик читает дешёвую метку и пересобирает список, только если она сменилась;
  Н-7 `blue-soft` и обводка `err` переехали в `DocPalette` (`BlueSoft`, `ErrLine`);
  Н-8 набор и порядок ключей frontmatter теперь в одном месте — `TaskFile.KEYS` + `fields()`,
  оттуда их берут и сборка файла, и `Edit`, и `ConflictRule`; Н-9 ключ `done` вместо значения
  статуса; Н-10 на 404 путь уходит из карты и владелец видит сообщение — призрака в списке не
  остаётся (новый тест); Н-11 `DESIGN.md` больше не числит расхождение по слоту `error`;
  Н-12 тег сносит только сторона крестика, тап по тексту безопасен; Н-13 нет токена → `failure()`,
  уточнение LLD-6.
  **Решение по Н-4 (ссылка на источник):** остаётся в Н5, критерий 6 спеки до него закрыт
  наполовину — задача показывает источник, но открывает его в GitHub. В приложении открыть
  нечего: кэш держит только `tasks/`, текста заметок в нём нет, а тождество «путь в репо ↔
  локальная запись» — это `NoteRef` (LLD-10), он приезжает со срезом ленты Н5. Тянуть заметку
  отдельным запросом ради одной ссылки — лишний путь чтения, который Н5 сразу же выбросит.
  Заведена задача `bd nikitatrubaev-0rk.13` (ждёт Н5) — чтобы критерий не потерялся.
  **Побочно:** `RepoStore` отдаёт экрану один снимок `view()` (метка, задачи, ждущие пути,
  сообщение) вместо четырёх вызовов подряд — из-за этого исчез дубль-`Snap` в `MainActivity`, а
  публичных функций у фасада стало 10 из 11 разрешённых, и `config/detekt/detekt.yml` не тронут:
  решение по послаблению остаётся за владельцем.
  **Неудачные попытки гейта:** (1) `ktfmtCheck` — 7 файлов не по формату после ручной правки,
  вылечено `ktfmtFormat`; (2) `detekt` — `TooManyFunctions`: у `RepoStore` стало 11 функций при
  пороге 11, вылечено сведением четырёх читателей в `view()`, а не правкой конфига;
  (3) `detekt` — `LoopWithTooManyJumpStatements` в `watchQueue`: `break` и `continue` в одном
  цикле, вылечено заменой `continue` на `null`-снимок.
  Живой `RepoSmokeTest` по-прежнему не гонялся — сеть до github.com рвётся
  (`nikitatrubaev-0rk.10`).

- 2026-08-29 — **фикс-цикл Н2, добавка: восемь находок поздних осей ревью и решение владельца по
  `detekt`**. Тестов 105 → **108**, падений 0, пропуска 2.
  **Решение владельца по `config/detekt/detekt.yml`:** послабление `TooManyFunctions` сужено до
  трёх файлов разметки экранов поимённо (`TaskSheets.kt`, `TaskDetailScreen.kt`,
  `TasksScreen.kt`). `ignoreAnnotated: ['Composable']` выброшен как ключ-пустышка (правило вешает
  находку на файл или класс, а они не аннотированы), `ignorePrivate: true` — как то, что глушило
  ровно `RepoStore` и `MainActivity`.
  **Фактура ревьюера оказалась неполной:** без `ignorePrivate` падают не шесть находок, а семь, и
  четыре из них — в тестовых классах (`RepoStoreWriteTest` 19, `FakeGithubApi` 12, `RepoCacheTest`
  и `TaskFileTest` по 11). Конфиг я не расширял: тестовым классам и фейку порта поставлено
  точечное `@Suppress` с причиной («тестовый класс — список проверок, а не поверхность класса»).
  `WriteQueue` (11 функций) закрыт удалением: перегрузка `pending(path)` оказалась мёртвой —
  её не звал никто, кроме себя самой.
  `RepoStore` (27 функций) и `MainActivity` (18) под порог честно не укладываются: это разделение
  фасада и отправки, то есть новая раскладка модулей и правка LLD-24, а не следствие Б1/Н-6.
  Заведена `bd nikitatrubaev-0rk.14`, обоим классам стоит точечное `@Suppress` со ссылкой на неё.
  Глобального послабления нет ни в конфиге, ни в коде.
  **Д1 (блокер, тихая потеря правки).** `sent()` выбрасывал операцию, если кэш потерял путь:
  янтарь гас, коммита нет, владельцу ни слова. Теперь база берётся из git (`readFile`), а если
  файла нет вовсе — это 404, то есть `vanished()` с сообщением. Тот же `?: drop(op)` убран из
  `conflict()`: базой становится текущий текст git, правка ложится поверх. Тест
  `кэш потерял путь — база берётся из git, правка не пропадает`.
  **Д2 (блокер, очередь вставала колом).** `Push.FAILED` не снимал операцию, и она запирала голову
  журнала навсегда — снять её владельцу нечем, `cancel` живёт пять секунд в снекбаре. Теперь наш
  баг снимает операцию и говорит владельцу, а цепочка всё равно гаснет `failure()`, чтобы баг был
  виден. Тест `наш баг снимает операцию, а не запирает очередь навсегда`.
  **Д3 (блокер, правка могла не уехать).** Планирование воркера висело на `LaunchedEffect` с
  пятисекундной задержкой: уход в деталку раньше снимал композицию, и правка лежала в журнале до
  следующей правки или перезапуска. Теперь отправку планирует `DisposableEffect { onDispose }` —
  срабатывает и по таймеру, и по «Отменить», и когда владелец ушёл с экрана.
  **Д5.** Седьмая проверка среза закрыта тестом `WorkChainsTest`: имена цепочек стали константами
  продакшна (`RepoWriteWorker.CHAIN`, `PipelineQueue.NOTE_PREFIX`), тест сторожит, что они не
  сольются ни при каком `noteId`. Что параллельные цепочки идут параллельно — гарантия
  WorkManager, её тест не перепроверяет, и в KDoc это сказано прямо.
  **Д6.** Доступность: чекбокс списка и подзадача — `toggleable` с `Role.Checkbox`, сегмент
  статуса — `selectable` с `Role.RadioButton`. TalkBack читает состояние, а не безымянную кнопку.
  **Д8.** Тавтология `assertTrue(keys.isNotEmpty())` в `EditTest` заменена на проверку порядка
  ключей целиком (включая хвостовой неизвестный `claude_hint`); `assertTrue(writeCalls <= …)` —
  на точное число попыток плюс проверку, что владелец получил сообщение о расхождении. Дубль
  `Triple`-блока в `MainActivity` исчез ещё в первой части цикла (`view()`), `TOUCH = 48` из
  четырёх файлов сведён в один `internal const` пакета `ui`; `SEGMENT` совпал с ним и удалён.
  **Мутации по двум новым блокерам** (правила `docs/harness/epic.md`), файл
  `RepoStore.kt`, sha256 в исходном виде `719a19f0`:
  Д1 — `?: api.readFile(op.path)` → `?: return drop(op)` (sha под мутацией `eeab0f16`) даёт ровно
  `1 failed`: `кэш потерял путь — база берётся из git, правка не пропадает`.
  Д2 — убрана строка `drop(op)` перед `Push.FAILED` (sha под мутацией `3f2908e8`) даёт ровно
  `1 failed`: `наш баг снимает операцию, а не запирает очередь навсегда`. После каждого отката
  копией файла sha снова `719a19f0`.
  **Д3 тестом не закрыт честно:** проверить, что `DisposableEffect` планирует отправку при уходе с
  экрана, нечем — тестов на Compose в репо нет вовсе (ни androidTest, ни Robolectric), и заводить
  их фикс-циклом — отдельный разговор. Правка проверена чтением кода; риск записан в отчёт.
  **Неудачная попытка гейта:** `detekt` после правки конфига — 8 находок (7 × `TooManyFunctions`
  плюс мой же `VariableNaming`: `private val HTTP_UNPROCESSABLE` в тесте вместо константы).
- 2026-08-29 — **повторное ревью Н2: CHANGES REQUESTED**, 1 блокер + 7 некритичных (вершина
  `c5800d2`, тестов 108, гейт зелёный, `bin/gate --full` — 46 секунд). Обе оси ревьюер снова
  гнал руками: сабагенты не ответили за 30 минут — третий случай подряд.
  **Блокер: Б1 вылечен наполовину.** Чтение снимка синхронизировано (метка `stamp()` работает,
  подтверждено мутацией), а запись — нет: `refresh()` перезаписывает ВЕСЬ снимок деревом,
  прочитанным до коммита воркера, `accept()` — тоже read-modify-write без синхронизации.
  Сценарий штатный: `store()` при непустой очереди зовёт `schedule()`, а `refreshRepo()` сразу
  следом — `refresh()`; оба сетевые, крутятся параллельно. Защита `path in waiting` делает хуже:
  в кэш ляжет старый `known[path]`, прочитанный до `accept()` воркера, операция к тому моменту
  снята с журнала, янтарь погас — и галочка отскочила ровно как в первом круге.
  **Ключевой факт, найденный ведущим:** `android:process` в манифесте нет, значит воркер
  WorkManager живёт в ТОМ ЖЕ процессе, что и экран. Гонка существует только потому, что
  `RepoWriteWorker.doWork()` строит себе собственный `RepoStore` (строка 41) вместо общего.
  Лечится не блокировками, а удалением второго экземпляра — один `RepoStore` на процесс.
  Некритичные: (1) «говорит владельцу» в Д2 не покрыто — мутация с удалением `say()` выжила,
  ни один из 108 тестов не упал; (2-4) починка тач-таргетов поехала по компу — чипы шторки
  создания дали ряд 48dp вместо ≈31, сегмент статуса вырос сам (44→48) вместо прозрачной
  полосы, `RemovableTag` накрывает короткий тег целиком вопреки своему же комментарию;
  (5) три побочки `DisposableEffect`: смена id тянет всю очередь мимо окна «Отменить», поворот
  экрана немедленно шлёт `onFlush()` и теряет `undo` (обычный `remember`, не `rememberSaveable`),
  снятие галочки планирует дважды; (6) `conflict()` кладёт чужой текст со старым `commitSha`;
  (7) `say()` перезаписывает `divergence.txt` — из нескольких сбоев виден только последний.
  Прошло проверку: конфиг detekt ровно как решил владелец, все семь `@Suppress` с честной
  причиной, Б3 (`Merged` — вердикт без данных, тест бьёт по байту в репо), Д1 (молчаливых
  `drop(op)` не осталось, `say()` идёт перед `drop`, `revision()` меняется — сообщение доходит).

- 2026-08-29 — **Н2, третий круг фикс-цикла** (вершина `7d55e62`, тестов 113, гейт зелёный).
  Блокер Б1 вылечен причиной, а не блокировками поверх симптома: `RepoStore.shared()` раздаёт
  **один фасад на процесс**, воркер и `MainActivity` берут его же (у `MainActivity` исчез
  `repoKey`); read-modify-write под монитором (`refresh`/`push` и `RepoCache.snapshot`/`save`
  — `@Synchronized`), замок держится ровно на один сетевой запрос. Уточнение записано в LLD-5.
  Тест `обновление параллельно с отправкой не теряет правку владельца` стартует отправку из
  хука фейка ровно между «дерево прочитано» и «снимок записан»; на старом коде падал строкой
  «ГАЛОЧКА ОТСКОЧИЛА». Попутная находка исполнителя: фейк не хранил блобы, а настоящий git
  отдаёт старый sha и после коммита — без этого гонка воспроизводилась не тем симптомом.
  Пять мутаций (снят `@Synchronized` с `refresh()`; выброшена сверка ключа в `shared()`; убран
  `say()` перед `drop(op)` в 422-ветке; `appendText`→`writeText`; возвращён `snapshot.commitSha`
  в `conflict()`) — каждая убила ровно свой тест, откаты копией.
  Комп восстановлен приёмом `TouchStripe` — прозрачная полоса 48dp **поверх** элемента через
  `matchParentSize` + `requiredHeight`, а не растягиванием видимого: чипы шторки вернулись к
  31dp, сегмент статуса к 38dp, крестик тега перестал накрывать текст.
  **Д9 — правка ведущего оказалась не в том месте**: узкий `excludes` в detekt ЗАМЕНЯЕТ
  дефолтный список, где стояли `'**/test/**'` и прочие тестовые globs, — отсюда и брались
  четыре `@Suppress` на тестовых классах. Globs возвращены, аннотации сняты; исполнитель
  проверил контролем (убрал три строки — detekt выдал ровно те же четыре находки).
  **Д10 — сознательное отступление, записано `ponytail`-комментарием:** строкам подзадач
  прозрачную полосу НЕ вешать. Строки идут вплотную, полосы соседей легли бы внахлёст, а в
  нахлёсте побеждает нарисованная позже — тап по нижнему краю переключал бы СЛЕДУЮЩУЮ
  подзадачу. Молча переключить не ту хуже, чем таргет 37dp у строки во всю ширину экрана.
  **Д12** — пункт 27 плана: три галочки спеки описывают цели заметки (файл / аудио / задачи),
  у одной задачи цель ровно одна и всегда отмечена, то есть галочка была бы лишним тапом перед
  необратимым действием, а не защитой. Галочки — в Н6, где целей правда несколько.
  Остальное закрыто кодом: Д2 «говорит владельцу» под утверждением, Д11 `contentDescription`
  больше не приписывает состояние невыбранному флажку, Д13 у `DeleteFile` своя формулировка,
  `say()` дописывает `divergence.txt` вместо перезаписи, `conflict()` кладёт чужой текст с
  пустым `commitSha`.
  **Д14, для владельца:** в `PipelineQueue` за весь срез изменены ровно две вещи — литерал
  `"note-"` вынесен в `internal const val NOTE_PREFIX` и подставлен в `beginUniqueWork`; имя
  цепочки побайтово то же, политика `KEEP`, воркеры, ограничения и бэкофф не тронуты.
  `RecordingService` и `TranscribeWorker` не тронуты вовсе. По букве плана прогон часовой
  записи (`nikitatrubaev-4wi.9`) обнулён; перегонять ли — решение владельца.
  Открытый риск: поворот экрана по-прежнему отправляет правку сразу, и дело не в
  `DisposableEffect` — `store()` при непустой очереди заводит воркер на холодном старте сам.
  Правка не теряется, теряется окно «Отменить». Заведено `nikitatrubaev-0rk.17`.

- 2026-08-29 — **третий круг ревью Н2: CHANGES REQUESTED**, 1 блокер + 6 некритичных
  (вершина `7d55e62`, тестов 113, гейт 3 секунды). Оси ревьюер гнал сам, как теперь и велит
  скилл.
  **Б1 закрыт наполовину третий раз — но каждый раз в НОВОМ месте.** Первый круг: чтение
  кэша. Второй: запись кэша. Третий: **журнал очереди**. `push()` под монитором, а `edit()`,
  `create()`, `cancel()` — нет; `push()` держит `op`, прочитанный в начале, и в конце делает
  `drop(op)` — удаление **по id**. Если владелец правит то же поле, пока PUT в полёте,
  `enqueue` склеивает новую правку в тот же id, и `drop` сносит её вместе с отправленной.
  Достижимо без экзотики: в деталке окна «Отменить» нет, `MainActivity.write()` зовёт
  `schedule()` сразу — тап P1, сразу P2, вторая правка съедена. Ревьюер доказал зондом на
  нетронутом `7d55e62`: `ГАЛОЧКА ОТСКОЧИЛА: снятие во время отправки съедено drop(op)`.
  **Осторожно с очевидным лекарством:** `@Synchronized` на `edit()` зонд чинит, но `push()`
  держит монитор через сеть (до трёх запросов, `TIMEOUT_MS = 60_000` и на connect, и на read)
  — тогда правка владельца будет ждать монитор минуту. Правильнее не расширять замок, а
  сделать `drop` условным: снимать операцию, только если она не менялась с момента чтения.
  Прошло проверку: второго писателя над кэшем действительно нет (`shared()` — единственный
  путь, голый конструктор только внутри него и в тестах на своих `tmp.newFolder()`); главный
  поток монитор не берёт; тест бьёт в тот самый шов; клип-риск `TouchStripe` чист — на пути
  нет ни `Card`, ни `clipToBounds`; Д9 воспроизведён контролем; Д2/Д11/Д12/Д13 под
  утверждениями.
  **Находка Н1, дороже всего остального: `TouchStripe` переизобретает штатный механизм.**
  У Compose в `NodeCoordinator.hitTest` есть ветка near-hit — промах мимо мелкого `clickable`
  в пределах `minimumTouchTargetSize` (48dp) засчитывается, и при споре соседей побеждает
  **ближайший**, а не нарисованный позже. `TouchStripe` подменяет это на «побеждает последний
  нарисованный» — и Н2 показывает цену: полоса крестика тега (48dp при чипе ≈25dp) вылезает
  на второй ряд тегов и молча сносит тег из первого. Если near-hit работает, полосы снимаются
  целиком, а решение Д10 (подзадачам полосу не вешать) перестаёт быть развилкой.
  Ещё: Н4 — `Source` остался с `height(TOUCH)` при компе ≈28px; Н5 — KDoc `refresh()` занижает
  потолок ожидания; Н6 — `takeDivergence()` без замка теряет сообщение воркера между
  `readText()` и `delete()`.

- 2026-08-29 — **Н2, четвёртый круг: блокер закрыт, и `TouchStripe` снят целиком**
  (вершина `f7184c3`, тестов 116, гейт зелёный).
  **Блокер — условное снятие вместо расширения замка.** Исполнитель подтвердил опасение
  ведущего (`push` держит монитор через сеть) и замок расширять не стал. `WriteQueue.done(op)`
  и `retry(op)` стали условными по содержимому: `unchanged(op)` сверяет прочитанный из файла
  `Op` обычным `equals` (все `Edit` — data-классы), поэтому склеенная во время PUT правка
  переживает отправку и уезжает следующим заходом. Короткое окно «прочитал файл → удалил»
  закрыто СВОИМ монитором журнала (`@Synchronized` на публичных методах `WriteQueue`) — сети
  под ним нет, микросекунды, и это не монитор `RepoStore`.
  **Н1 — near-hit Compose работает штатно, проверено байткодом, а не памятью.** Разобран
  `ui-release.aar` 1.7.6 и `material3-release.aar` 1.3.1 из `~/.gradle/caches` (`javap -p -c`,
  sources-jar нет): радиус `ViewConfiguration.minimumTouchTargetSize` = `DpSize(48,48)`,
  default-метод интерфейса, `AndroidViewConfiguration` его не переопределяет;
  `DistanceAndInLayer.compareTo` сравнивает сперва «внутри слоя», затем `signum(distance)` —
  побеждает **ближайший**, отрисовка решает только ничью; настоящее попадание (`-1f`) сильнее
  любого near-hit; opt-in не требуется, условие одно — `PointerType.Touch`.
  `minimumInteractiveComponentSize()` не подошёл бы: отдаёт `max(placeable.height, 48dp)` в
  `layout()`, то есть двигает разметку. **`TouchStripe` удалён**, `clickable`/`selectable`
  вернулись на сами элементы, видимая геометрия не изменилась ни на dp.
  Вместе с полосой растворились Н2 (крестик тега больше не настоящее попадание на 48dp) и
  развилка **Д10** — строки подзадач кликабельны во всю ширину, соседний near-hit их не
  перебьёт. Решение Д10 и `ponytail`-комментарий у `Subtasks` больше не нужны.
  Н4: `Source` переведён на комповый `padding(top = 10.dp)` при собственной высоте ≈18dp.
  Н5: KDoc `refresh()`/`push()` назвал настоящий потолок — минуты, и почему это терпимо
  (главный поток монитор не берёт вовсе). Н6: `takeDivergence()` и `say()` под своим монитором
  `notices`; переименование файла пробовалось первым и НЕ спасает — `appendText`, открывший
  дескриптор до rename, пишет в переименованный inode, тест падал.
  Мутации: снято условие в `done` и снят `@Synchronized` с `done` (`WriteQueue`), снят
  `synchronized(notices)` (`RepoStore`) — каждая убила ровно свой тест; управляющая (`edit()`
  не кладёт в очередь) убила четыре, харнесс жив.
  Остаток: тап по левому краю чипа второго ряда тегов, если прямо над ним крестик первого —
  мазок 18dp туда дотягивается, конкурента нет (текст чипа кликабельным делать нельзя).
  Заведено `nikitatrubaev-0rk.18`. Мышь и стилус near-hit не получают (`isTouchEvent = false`)
  — на телефоне владельца не встречается.

- 2026-08-29 — **четвёртый круг ревью Н2: CHANGES REQUESTED**, но цепочка гонок ЗАКРЫТА
  (вершина `f7184c3`, тестов 116, гейт 1 секунда).
  **Четвёртого писателя нет — главный вопрос закрыт.** Сплошной `grep` по `app/src/main`
  (`writeText|appendText|delete()|renameTo|mkdirs|outputStream`): в `files/repo/` пишут ровно
  трое — `RepoCache.save()`, `WriteQueue`, `say()`/`takeDivergence()`; всё остальное
  (`PushWorker`, `TranscribeWorker`, `NotesStore`, `RecordingService`) пишет в `files/notes/`.
  Все трое достижимы только через один экземпляр `RepoStore` (`shared()` — единственный путь
  в проде, два вызова). Окна «прочитал → изменил → записал» закрыты, взаимной блокировки нет:
  `RepoStore` — единственный внешний монитор, `WriteQueue`/`RepoCache`/`notices` — листья, а
  `edit/create/delete/cancel/view` монитор `RepoStore` не берут вовсе.
  **Near-hit перепроверен вторым `javap` независимо — заявление верно.**
  `getMinimumTouchTargetSize` — default-метод с телом `DpSize(48,48)`, `AndroidViewConfiguration`
  его не переопределяет; единственный гейт near-hit-ветки — булев `isTouchEvent`, opt-in нет;
  `compareTo` — сперва `isInLayer`, потом ближайший. Оговорка: `-1f` сильнее near-hit только
  ВНУТРИ одной корзины `isInLayer`.
  **Новый блокер, и он — цена сноса `TouchStripe`.** `calculateMinimumTouchTargetPadding` =
  `max(0, (48 − measured)/2)` на каждую сторону по ОБЕИМ осям. Крестик тега меряется 12×12,
  значит мазок near-hit равен 18dp и по горизонтали тоже; текст чипа сознательно не кликабелен,
  конкурирующего настоящего попадания в чипе нет. Для короткого тега (`#a`, `#дом`) весь чип с
  именем лежит внутри 18dp-коробки крестика → **тап по имени молча сносит тег**, окна «Отменить»
  на деталке нет. `TouchStripe` это как раз не ломал: он был `matchParentSize` поверх Box 12×12,
  то есть 12dp в ширину и до текста не доставал. `0rk.18` покрывает только второй ряд — это
  другой случай.
  Некритичные: (1) условие в `retry` не покрыто ничем — мутация `if (unchanged(op)) write(next)`
  → `write(next)` **выжила**, 0 падений из 116; сторожит ту же потерю правки, что и `done`;
  (2) при смене токена, пока воркер тянет очередь, экран получает новый фасад, а воркер до конца
  `doWork()` держит старый — все три монитора раздваиваются, включая коллизию на фиксированном
  `repo.json.tmp`; (3) **`RepoStore.today` заморожен конструктором**, а экземпляр теперь живёт
  весь процесс: после полуночи новая задача получает вчерашнюю дату в имени файла и в `created:`,
  а экран рисует `LocalDate.now()` — следствие синглтона третьего круга; (4) `cancel(id)` остался
  безусловным снятием по id: «ОТМЕНИТЬ» при летящем PUT убирает запись, но коммит доезжает —
  отмена молча не срабатывает; (5) рипл чипа прямоугольный на скруглённом 9dp (`clip` перед
  `clickable`); (6) две неточности в KDoc.
  Геометрия по компу сверена и не поехала: чип 31dp, сегмент 38dp, подзадачи не тронуты, `Source`
  переведён на `.momentlink` — это заявленный Н4, не регресс.

- 2026-08-30 — **живой смоук прогнан, открытый риск Н1 снят** (`nikitatrubaev-0rk.10`).
  `NOTEAPP_SMOKE_TOKEN=$(gh auth token) bin/gate testDebugUnitTest --tests '*RepoSmokeTest*'`
  — зелёный с первой попытки, 20 секунд, ретраи не понадобились (`curl /rate_limit` перед
  прогоном: 200 за 0,56 с — сеть держала все девять последовательных запросов).
  Прочитано из `Roflochinsky/voice-notes-test`: шесть задач в `tasks/` с правильными рубриками
  и просрочками — ровно то, что ревьюер Н1 вытаскивал через `curl`; плюс заметка по
  кириллическому пути `встречи/2026-08-24-1807-reliz-tgsum.md` разобралась.
  **Чем это закрывает риск:** боевой `httpGet` в JVM прогнан живьём — заголовки
  `Authorization`/`Accept`, TLS, цепочка `git/ref` → `git/trees?recursive=1` → `git/blobs`
  (`getMimeDecoder` на переносах строк в `content`) и `contents/` с процентной кириллицей.
  До сих пор он был проверен только глазами по диффу. Дефектов боевого пути нет.
  Побочно подтверждено намеренное поведение: «Сделано за месяц · 1» при двух строках `[x]` —
  `TaskFilter.doneCount` считает только закрытые с датой внутри окна, а закрытые без даты
  показываются, но не считаются (буква ADR).

- 2026-08-30 — **Н2, пятый (последний) круг: тап по имени тега больше не сносит тег**
  (влит main `5fcaf83`, тестов **118**, падений 0, пропуска 2 — смоуки без токена,
  `bin/gate --full` зелёный).
  **Блокер — чипу дан свой НАСТОЯЩИЙ конкурент.** Near-hit крестика симметричен по обеим осям
  (`max(0, (48 − 12)/2)` = 18dp на сторону), поэтому у короткого тега он накрывал и имя, а
  конкурента в чипе не было вовсе. Увеличить крестик до 44dp комп не даёт, урезать радиус
  near-hit можно только подменой `LocalViewConfiguration` — это ударило бы по всему поддереву.
  Поэтому чип глотает тап `pointerInput`-ом: настоящее попадание в своей корзине `isInLayer`
  сильнее любого near-hit, а крестик лежит ГЛУБЖЕ чипа и поэтому свой тап получает по-прежнему.
  `clickable` на чипе не годился — объявил бы чип кнопкой для TalkBack и дал рипл там, где тап
  ничего не делает. Видимая геометрия не изменилась ни на dp (глушилка стоит до `padding`).
  Побочно закрылся и случай `nikitatrubaev-0rk.18`: у чипа второго ряда теперь тоже есть
  настоящее попадание. Остался 2dp зазора `spacedBy` между рядами — там настоящего попадания
  нет, тап достаётся крестику ряда выше.
  **Условие в `retry` закрыто тестом** `повтор после конфликта не затирает правку, склеенную во
  время отправки`: отправленная операция, склеенная владельцем в тот же файл журнала, не
  переписывается старой (`WriteQueueTest`).
  **`RepoStore.today` разморожен.** Дата берётся в момент использования (`clock: () -> LocalDate`
  плюс геттер `today`), а не в конструктор: фасад один на процесс и живёт дольше суток, иначе
  задача, заведённая после полуночи, получала вчерашнее имя файла, вчерашний `created:` и
  вчерашний `done:`, а экран рисовал сегодняшнюю дату.
  **Мутации** (правила `docs/harness/epic.md`, откат копией файла): `WriteQueue.kt`
  (`b9246003` → `ea9e64c3` → `b9246003`) — снято условие в `retry`, ровно `1 failed`
  `повтор после конфликта не затирает правку, склеенную во время отправки`; `RepoStore.kt`
  (`51638684` → `d9843925` → `51638684`) — `today` заморожен обратно в конструктор, ровно
  `1 failed` `после полуночи дата новая, а не та, с которой открыли приложение`. Третья мутация
  (`TaskDetailScreen.kt`, `9b88539a` → `83a81557` → `9b88539a`, снята глушилка чипа)
  **выжила — и это ожидаемо**: Compose-тестов у проекта нет, а завести их офлайн нечем —
  ни `robolectric`, ни `compose ui-test` в `~/.gradle/caches` не лежат (`nikitatrubaev-0rk.16`).
  UI-правка проверена сборкой и разбором hit-теста Compose из прошлых кругов, глазами на
  устройстве — нет. **Управляющая** мутация (`edit()` возвращает константу вместо постановки в
  очередь, `7e8b4e83`) убила восемь тестов — харнесс жив.
  Остаток четвёртого круга (два фасада при смене токена, безусловный `cancel(id)`, прямоугольный
  рипл чипа, неточности KDoc) заведён одной задачей `nikitatrubaev-0rk.19`.

- 2026-08-30 — **Н3 сделан** (ветка `slice-h3`): миграция, батч-коммит, реестры, чипы и поиск.
  Гейт `bin/gate --full` зелёный: тестов **155** (на базе было 119), падений 0, пропусков 4 —
  все четыре смоука без токена; APK собран.
  **Живой прогон миграции против тестового репо прошёл целиком.** Сухой прогон печатает список
  («заметка `идеи/2026-08-12-…` → `tasks/2026-08-12-posmotret-limity-notion-api.md`, итого задач
  1, файлов в коммите 2»); настоящий прогон лёг **одним** коммитом
  [`bd259022`](https://github.com/Roflochinsky/voice-notes-test/commit/bd259022ccad78b130a78a8a70ed57b77abd2fc2)
  ровно на два файла (`added tasks/…`, `modified идеи/…`); повторный план на мигрированном репо
  пуст; обратная пачка (`revert: migration: tasks v2`) вернула дерево — карта `путь → blobSha`
  сошлась с исходной побайтово, фикстура немигрированного чекбокса цела.
  **Решения, принятые в исполнении (для ревью):**
  1. **Блобы отдельными запросами не создаются** — содержимое едет полем `content` прямо в записи
     дерева. Это буква research §7.2 («Если содержимое тоже меняется — в записи дерева вместо `sha`
     можно передать `content` напрямую; отдельный `POST /git/blobs` тогда не нужен») и на файл
     экономит по запросу. Формулировка среза «blobs → tree → commit → PATCH ref» — про путь git
     data API, и он соблюдён: `ref` → `git/commits/{head}` → `POST git/trees` → `POST git/commits`
     → `PATCH refs/heads/main` с `force:false`, одна пересборка на 422.
  2. **Дата задачи при миграции — из заметки** (`recorded`, иначе дата в имени файла), не из
     сегодня: иначе сухой прогон и настоящий, сделанные в разные дни, дали бы разные имена файлов.
     Заметка с чекбоксами, у которой даты нет вовсе, **не мигрируется** и попадает в `skipped`
     сухого прогона — молча писать в боевой репо файл с выдуманной датой нельзя.
  3. **Задача наследует `project` заметки, теги — нет.** Проект принадлежит работе (без него чипы
     для мигрированных задач пусты), а тег описывает заметку и из текста чекбокса не выводится:
     лишняя метка в боевом репо хуже отсутствующей.
  4. **`done`-чекбокс переносится как `status: done` без поля `done`** — буква ADR: даты закрытия в
     v1 не было, а выдумывать её ADR запрещает («показывается в конце секции без даты»).
  5. **Чекбокс мигрируется только внутри секции «Задачи»** (`**Задачи.**` / `## Задачи`, до пустой
     строки, `#`-заголовка или следующего жирного блока). Чекбокс в «Ключевом» — чужой текст
     владельца, трогать его миграция права не имеет.
  6. **Статус-чип по умолчанию сброшен** («Статус ⌄»), хотя комп рисует его активным «Открытые».
     Комп показывает состояние, а не умолчание: на борде 5 активен «Персона: Дима», что умолчанием
     быть не может. Умолчание «сброшено» сохраняет экран Н1 как есть (рубрики + «Сделано»), а
     выбор «Открытые» рисуется ровно как в компе — синий, с крестиком сброса.
  7. **Шов записи у `GithubClient`** (`write: (метод, URL, тело) -> String`) — иначе цепочка батча
     и пересборка на 422 проверялись бы только живой сетью. Фикстуры ответов настоящие: `GET
     git/commits/{sha}` снят обычным чтением, а `POST git/trees` и `POST git/commits` — реальными
     запросами, которые создают **висячие** объекты и ветку не двигают (HEAD до и после совпал).
  8. **PATCH и JVM.** `HttpURLConnection` в OpenJDK метод `PATCH` не принимает, на Android
     (OkHttp) принимает; `java.net.http.HttpClient` в юнит-тестах Android-модуля невидим —
     `android.jar` в bootclasspath. Поэтому JVM-смоук миграции подставляет свой шов на внешнем
     `curl` (токен уходит файлом конфигурации с правами 600, не в argv). Боевой путь приложения не
     тронут, но и не проверен ни разу — `bd nikitatrubaev-0rk.24`.
  **Мутационная проверка** (правила `docs/harness/epic.md`, откат копией файла):
  - **Фасетный счётчик чипов** — `TaskFilter.kt` (`802d47dc` → `86fc9c62` → `802d47dc`): в
    `Filter.counts` снято исключение своего фасета (`with(facet, null).select(tasks)` →
    `select(tasks)`). Ровно `1 failed`, и это `счётчик в шторке фасетный — свой фильтр в расчёт
    не идёт`.
  - **Идемпотентность миграции** — `Migration.kt` (`42c5d16f` → `bdd9390c` → `42c5d16f`):
    регексп чекбокса стал считать чекбоксом любые скобки (`\[([ xX])]` → `\[([^\]]+)]`), то есть
    ссылка `- [Заголовок](../tasks/…)` снова выглядит немигрированной. Упало **четыре** теста из
    155, и все четыре — про это свойство: `уже мигрированная заметка со ссылками не мигрирует
    повторно`, `второй прогон поверх собственного результата не меняет ничего`, `чекбокс
    настоящей заметки становится файлом задачи и ссылкой на него`, `имя задачи занято — берётся
    суффикс`. Правило «ровно 1 failed» тут недостижимо честно: свойство «ссылка — не чекбокс»
    закреплено четырьмя тестами сразу, и любая мутация, которая его ломает, валит все четыре.
  - Третья, попутная: `BatchPlan.kt`, `sha: null` → `sha: ""` — упали ровно два своих теста
    (`удаление — это sha null…`, `переименование — удаление плюс создание…`).
  - **Управляющая**: `Filter.select` возвращает список нетронутым — упало семь тестов, харнесс жив.
  **Неудачные попытки гейта:** (1) `ktfmtCheck` — трижды за срез, каждый раз после ручной правки
  (лечится `ktfmtFormat`); (2) `detekt` — `LongParameterList` у `Migration.task` (шесть
  параметров: `notePath` оказался лишним, `NoteFile.Note` уже носит свой `path`), `TooManyFunctions`
  у `GithubClient` (11 при пороге 11 — растворён однострочный `send`, сборка URL `contents` уехала
  в компаньон) и у `TaskFilter` (12, затем 11 — `apply`/`counts`/`matches` переехали членами в
  `Filter`, где правилу не видны); конфиг detekt не тронут, лечили кодом, как в фикс-цикле Н1;
  (3) `GithubBatchTest` красный на первой сборке: снятые фикстуры `git/commits` и `POST git/trees`
  были сделаны с текущего HEAD, а `ref.json` в репо — со старого; пересняты с того коммита, на
  который смотрит `ref.json`, чтобы весь набор был из одного состояния репо.
  **Тронуто в чужой зоне (минимально):** `RepoStore` — `refresh()` кладёт в кэш ещё и
  `projects.md`/`people.md`, `View` получил поле `projects`; `MainActivity` — поле `registry`,
  `projects()` отдаёт реестр плюс проекты из задач, `TasksScreen` получает `projects`;
  `SectionTabs` — необязательные параметры поиска (лента их не передаёт и не изменилась);
  `app/build.gradle.kts` — вывод в консоль гейта теперь у любого `*SmokeTest`, а не только у
  `RepoSmokeTest`. `TaskDetailScreen.kt`, `TaskSheets.kt`, `FeedScreen.kt`, `DetailScreen.kt` не
  тронуты; `ChoiceSheet` переиспользован как есть.
  **Отложено с задачами:** `nikitatrubaev-0rk.23` (строка «Новый проект» из компа — реестр умеет
  только читать), `.24` (PATCH с телефона), `.25` (чипы «Тег» и «Срок» — комп рисует пять, срез
  требовал три), `.26` (заголовок шторки прописными вопреки компу — живёт в `TaskSheets.kt`).
  **Открытые риски:** Compose-тестов у проекта по-прежнему нет (`nikitatrubaev-0rk.16`), поэтому
  чипы, шторки, поиск и пустое состояние «под фильтром» проверены сборкой и разбором, а не
  прогоном на экране; поиск ищет по заголовку задачи — превью заметок ждёт ленты (Н5).

## Ретро

_(в режиме 3)_
