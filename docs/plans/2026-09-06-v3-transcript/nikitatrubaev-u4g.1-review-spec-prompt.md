# Ревью среза nikitatrubaev-u4g.1 — ось «СПЕКА»

Ты ревьюер, не автор. Ты проходишь **одну ось — «спека»**; ось «стандарты» параллельно идёт
другим агентом в этом же дереве, ваши находки сводит ведущий. Молчание о пункте = пункт не
пройден.

**Файлы в дереве не правишь вообще** (в нём же читает вторая ось) и `scripts/mutate.py` не
запускаешь: полный набор мутаций на сданном HEAD прогнал ведущий, его таблица дана ниже как
вход. Твоё дело — судить таблицу и покрытие, а не пересобирать её.

## Что смотрим

Дифф ветки `nikitatrubaev-u4g.1` против `main` в дереве
`/home/nikitatrubaev/orca/workspaces/noteapp/nikitatrubaev-u4g.1`
(`git -C <дерево> diff main...HEAD`).

## Входы

- Спека (часть 1, подписана владельцем):
  `/home/nikitatrubaev/code/noteapp/docs/specs/2026-09-06-v3-transcript.md`
- **План — читай целиком**, особенно «Ревью замысла» (решения Р1–Р8) и «LLD среза `u4g.1`»:
  `/home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript.md`
- ADR: `/home/nikitatrubaev/code/noteapp/docs/adr/2026-09-06-stt-elevenlabs-scribe-v2.md`
- Задача и строка «Трогает:»: `bd show nikitatrubaev-u4g.1`
- Задание исполнителя (что ему было сказано дословно):
  `/home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/nikitatrubaev-u4g.1-prompt.md`
- Журнал исполнителя (его сдача):
  `/home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/nikitatrubaev-u4g.1.md`
- Вывод гейта: ``/tmp/claude-1000/-home-nikitatrubaev-code-noteapp/f6448f97-85c9-429a-8a98-7b14a4fd648a/scratchpad/nikitatrubaev-u4g.1-gates.txt``
- Таблица мутаций ведущего: ``/tmp/claude-1000/-home-nikitatrubaev-code-noteapp/f6448f97-85c9-429a-8a98-7b14a4fd648a/scratchpad/nikitatrubaev-u4g.1-mutation-table-full.md` (полный прогон ведущего, rc=0; сырой вывод — `/tmp/claude-1000/-home-nikitatrubaev-code-noteapp/f6448f97-85c9-429a-8a98-7b14a4fd648a/scratchpad/nikitatrubaev-u4g.1-mutations-full.txt`, спека — `/tmp/claude-1000/-home-nikitatrubaev-code-noteapp/f6448f97-85c9-429a-8a98-7b14a4fd648a/scratchpad/nikitatrubaev-u4g.1-mutations-ctl2.toml`)`
- Боевой ответ, из которого взята структура фикстуры (личный текст, в репо его быть не должно):
  `/home/nikitatrubaev/stt-bench-out/09-elevenlabs-bez.json`
- Правила вердикта и классов: `/home/nikitatrubaev/code/noteapp/docs/harness/epic.md`

## Рубрика оси «спека»

Дифф против спеки, плана и ADR:

- запрошенное поведение отсутствует или сделано частично?
- скоуп-крип — сделано то, чего не просили?
- технически расходится со спекой или планом?
- **нарушен принятый ADR** — назови файл ADR и место.

Отдельно проверь, что выполнены решения ревью замысла Р1–Р8 (они обязательны): Deepgram из
пайплайна ушёл и ветки «выбор клиента по ключу» нет; клиент со швом транспорта, а не `object`;
диспетчера разбора по форме JSON нет; номер спикера — цифра из `speaker_id`; тело multipart
стримится, а не собирается в память; `keyterms` в срезе нет; `fromDeepgramJson`,
`DeepgramClient.kt`, `WorkChainsTest`, `OnboardingScreen.kt`, `app/build.gradle.kts` не тронуты.

## Твои пункты вердикта

1. **ГЕЙТЫ.** Гейт ПРИНИМАЕШЬ по sha из журнала: HEAD дерева совпал с заявленным, копия чистая,
   своих правок нет, файл вывода есть → напиши «приняты по `<sha>`». Не совпало — скажи это,
   сам гейт не гоняй (машина занята второй осью), доложи ведущему.
2. **СПЕКА.** По строке на КАЖДЫЙ из критериев приёмки 1–4 (критерии 5–6 — срез `u4g.2`, они
   вне этого среза): `<спека>:<строка>` → тест по ИМЕНИ либо «нет теста, ловится <чем>». Без
   пропусков. Тест, который проходит и на неверной реализации, за покрытие не считается — скажи
   это прямо.
3. **МУТАЦИЯ.** Суди таблицу ведущего: управляющая убита? по строке на мутацию с именем упавшего
   теста? **Каждая ли мутация разделяющая** — то есть различает верную семантику и ближайшую
   неверную, а не ломает всё подряд? Выжившая мутация = `CHANGES REQUESTED` без обсуждения.
   Если считаешь, что нужна мутация, которой в наборе нет, — назови её текстом (`file`, `find`,
   `replace`, `tests`, `expect_fail`), ведущий прогонит.
4. **ФОРМАТ/ПРИВАТНОСТЬ.** Срез трогает файл репо заметок, аудио или ключи? По каждому — какой
   ADR это разрешает и какой тест держит контракт. **Отдельно и обязательно:** репозиторий
   `Roflochinsky/noteapp` публичный, а фикстура собрана из боевого ответа с личным разговором
   владельца — проверь сам, своим способом, что в `app/src/test/resources/elevenlabs-sample-response.json`
   не осталось текста разговора (решение Р2 плана). Доказательство исполнителя перепроверь, не
   принимай на слово.
5. **СКОУП-КРИП.** Изменённые файлы против строки «Трогает:» из bd; вне списка — перечислить или
   «нет». Расхождение — обязательная находка.

## Форма отчёта

Пиши нарастающим итогом в файл основного дерева через шелл (инструмент `Write` в чужое дерево не
пускает):

```
cat > /home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/nikitatrubaev-u4g.1-review-spec.md <<'EOF'
...
EOF
```

Шапка: `## Ревью среза nikitatrubaev-u4g.1 — ось СПЕКА`, строка «ревьюер, модель, усилие,
дерево», затем пункты 1–5 выше, затем:

**НАХОДКИ.** `<файл>:<строка>` — что — почему нарушение (правило и где записано) — предлагаемый
класс (`a`/`t`/`b`/`c`/`d`/`e` по таблице `docs/harness/epic.md`; класс утверждает ведущий).
Либо «находок нет». **Сообщай ВСЕ находки, без отбора по важности** — фильтрация это работа
ведущего. Находка без приложенной мутации или замера будет понижена до `b`, поэтому прикладывай.

Итог одной строкой: `APPROVE` | `CHANGES REQUESTED` (только за конкретное неверное поведение с
примером вход → выход) | `ВЛАДЕЛЬЦУ`.

Порог `CHANGES REQUESTED`: класс `a`; `t`, прикрывающая `a` того же среза; `e`, если предмет
среза и есть документ. Остальное — `APPROVE` с находками в вердикте.

Своих сабагентов не запускай. Отчёт в чат — короткий: вердикт, число находок, путь файла.
