# Второе ревью среза nikitatrubaev-u4g.2 (уровень П) — ось `<ОСЬ>`

Уровень **П** (полное второе ревью) обязателен: в круге была находка класса `a` — продукт врал
владельцу — и прод-кода тронуто больше 20 строк.

**Важное про твой контекст.** Первый круг ревью проходили другие агенты, их вердикты лежат
файлами и даны тебе входом. Харнес предписывает второй круг ТЕМ ЖЕ ревьюером в продолжении
контекста; здесь это невозможно — машина ушла в нехватку памяти, и удержанные ревьюеры были
освобождены, чтобы фикс-воркер не убило. Поэтому читай вердикты первого круга как чужие: проверяй
их выводы, а не принимай на веру.

## Что смотрим

Дерево `/home/nikitatrubaev/orca/workspaces/noteapp/nikitatrubaev-u4g.2`, ветка
`nikitatrubaev-u4g.2`.

- Дифф всего среза: `git -C <дерево> diff main...HEAD`
- Дифф круга фикса: `git -C <дерево> diff 235f5a3..52bb9b6`
- Сданный HEAD: **`52bb9b6`** (`235f5a3` — срез, `52bb9b6` — круг фикса)

## Входы

- Вердикты первого круга (в них полный текст находок): `…/nikitatrubaev-u4g.2-review-spec.md`,
  `…/nikitatrubaev-u4g.2-review-std.md`
- Задание круга фикса (что было велено закрыть и какие решения принял ведущий):
  `…/nikitatrubaev-u4g.2-fix-prompt.md`
- Журнал исполнителя целиком, с разделом круга фикса: `…/nikitatrubaev-u4g.2.md`
- Спека (критерии 5 и 6), план (раздел «LLD среза `u4g.2`»), ADR — пути в журнале.
- Гейт круга фикса (sha первой строкой) и прогон мутаций лежат в скратчпаде **исполнителя**:
  `/tmp/claude-1000/-home-nikitatrubaev-orca-workspaces-noteapp-nikitatrubaev-u4g-2/95e00601-7bd1-40cf-837f-8a0651d97097/scratchpad/`
  — файлы `nikitatrubaev-u4g.2-gates-fix.txt`, `nikitatrubaev-u4g.2-mutations-fix.txt`,
  `nikitatrubaev-u4g.2-mutations-fix.toml`.
- Правила вердикта и классов: `/home/nikitatrubaev/code/noteapp/docs/harness/epic.md`

Все пути к документам среза — в каталоге
`/home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/`.

## Правила

Файлы в дереве **не правишь** (в нём же читает вторая ось), `bin/gate` и `scripts/mutate.py` **не
запускаешь**: гейт и полный набор из тринадцати мутаций с управляющей прогнаны, вывод дан входом.
Гейты принимаешь по sha. Сообщаешь ВСЕ находки с предлагаемым классом; класс утверждает ведущий.

## Что сделать

1. **Блокер закрыт?** Класс `a` первого круга: после ввода ключа лента до пяти часов говорила
   «нет ключа ElevenLabs», потому что `ExistingWorkPolicy.KEEP` выбрасывал новый запрос, а
   `status.txt` никто не снимал. Фикс: `enqueueWaiting` снимает протухшую причину **и** отменяет
   висящую цепочку перед постановкой. Посмотри тест и мутации `M10`, `M11` — что они ВИДЯТ, и
   закрыт ли сценарий целиком, включая тап «Повторить» в том же окне.
2. **По каждой находке первого круга своей оси — «закрыта / не закрыта», с доводом.** Не по
   журналу исполнителя: открой тест, который её якобы закрывает.
3. **Перепройди свою ось целиком на новом HEAD.** Круг фикса тронул прод-код в четырёх файлах —
   новые находки возможны. Пункты формы вердикта своей оси пройди заново.
4. **Посуди набор мутаций.** Их стало тринадцать плюс управляющая, все убиты, `--scope full`
   rc=0, полный прогон под управляющей дал ровно одно падение. Вопрос твоей оси: **разделяющая
   ли каждая**, и не осталось ли решения боевого пути, которое не бьёт ни одна.
5. **Отдельно посуди отказ исполнителя** взять в набор мутацию `M10-статус-уезжает-в-репо` из
   вердикта первого круга: она по построению обязана ВЫЖИТЬ, а `mutate.py` считает выжившую дырой
   и возвращает rc=1. Исполнитель оставил это решением ведущего. Скажи, согласен ли, и как
   доказать это свойство иначе.
6. **Новый файл вне строки «Трогает:»** — `app/src/test/kotlin/com/roflochinsky/noteapp/pipeline/NotesStoreTest.kt`
   (в репо его не было вовсе). Исполнитель объявил его и объяснил, почему не положил стража в
   `MainActivityTest`. Скоуп-крип это или верное место — скажи прямо.
7. Решения, которые обсуждать не надо, они приняты ведущим: пустой ответ вендора остаётся в
   очереди (`retry`) без причины; `ExistingWorkPolicy.KEEP` в `PipelineQueue` не меняется; долг
   «проводка `MainActivity` исполняется наполовину» остаётся долгом. Считаешь неверным — скажи
   находкой, а не блокером.

## Отчёт

Пиши нарастающим итогом через шелл (`Write` в чужое дерево не пускает):

```
cat > /home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/nikitatrubaev-u4g.2-review2-<ось>.md <<'EOF'
...
EOF
```

где `<ось>` — `spec` или `std`, та, что назначена тебе задачей. Шапка: `## Второе ревью (П) среза
nikitatrubaev-u4g.2 — ось <ТВОЯ>`, строка «ревьюер, модель, усилие, дерево, HEAD».

Разделы: «Блокер класса `a` — закрыт или нет»; «Находки первого круга моей оси — закрыта / не
закрыта»; пункты формы вердикта заново; «Новые находки» с классами либо «новых находок нет»; итог
одной строкой `APPROVE` | `CHANGES REQUESTED` (только за конкретное неверное поведение с примером
вход → выход) | `ВЛАДЕЛЬЦУ`.

Своих сабагентов не запускай. В чат — коротко: вердикт, закрыт ли блокер, сколько новых находок.
