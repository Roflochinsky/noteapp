## Ревью среза nikitatrubaev-u4g.1 — ось СТАНДАРТЫ

Ревьюер: воркер Orca (ось «стандарты», вторая ось идёт отдельным агентом) · модель `claude-opus-5` ·
усилие high · дерево `/home/nikitatrubaev/orca/workspaces/noteapp/nikitatrubaev-u4g.1`.
Файлы дерева не правил, `bin/gate` и `scripts/mutate.py` не запускал.

### 1. ГЕЙТЫ

**Приняты по `05ea1eb5ca63862fe261bc23d4bc59500cf8ed2c`.**

- `git rev-parse HEAD` дерева = `05ea1eb5ca63862fe261bc23d4bc59500cf8ed2c` — совпал с заявленным
  в журнале (раздел 1 и строка «гейты:»).
- `git status --porcelain` пуст: копия чистая, своих правок в дереве нет.
- Файл вывода на месте: `…/scratchpad/nikitatrubaev-u4g.1-gates.txt` (3792 Б, 20:27), первая
  строка — `gate: --offline --no-build-cache ktfmtCheck detekt lint cleanTestDebugUnitTest
  testDebugUnitTest assembleDebug`, последние — `BUILD SUCCESSFUL in 22s`, `3 executed, 54
  up-to-date`. `> Task :app:testDebugUnitTest` в выводе без `FROM-CACHE` и без `UP-TO-DATE` —
  прогон настоящий, ловушка Gradle из `docs/harness/epic.md` не сработала. Коммит сделан в
  20:24:57, mtime исходников 20:21 — файл вывода моложе кода, соответствие по времени сходится.
- Мутации ведущего: `…/nikitatrubaev-u4g.1-mutations-full.txt` — M1–M5 убиты, управляющая
  (`FILENAME` `audio.m4a` → `audio.bin`) убита, полный прогон под управляющей убит.

Две оговорки к принятию, обе не блокирующие:

- **Числа «310 тестов, 0 падений» в приложенном выводе не видно** — Gradle их не печатает.
  Проверил по XML-отчётам дерева: 39 классов, 310 тестов, 7 пропущено. Единственное падение в
  отчётах — `ElevenLabsClientTest::запрос — шесть полей multipart…` с `filename="audio.bin"`, то
  есть это **управляющая мутация ведущего** (прогон 20:35, позже гейта), а не красный тест среза.
  Оговариваю прямо: каталог `app/build/test-results` сейчас хранит мутированный прогон, поэтому
  «зелёный на сданном HEAD» я принимаю по файлу вывода, а не по этим XML.
- Строка журнала «`bin/gate --full` rc=0 50 с» и приложенный файл — **разные прогоны**: в файле
  перезапуск `--no-build-cache` на 22 с (журнал пишет 21 с). Расхождение объявлено самим
  исполнителем (раздел 2 журнала) и объяснено честно; `rc=` в файле нет, зелёное подтверждается
  строкой `BUILD SUCCESSFUL`. Разночтение 21 против 22 с — округление, не находка.

### 2. ГРАНИЦЫ — что стражи ВИДЯТ

Список обходов никогда не полный; ниже — ровно то, что тест видит, и ровно то, что не видит.

**Видят.**

- `ElevenLabsClientTest` (через шов `post`): адрес `…/v1/speech-to-text`, заголовок `xi-api-key`,
  **карта полей целиком** — лишнее поле валит тест (сравнение `assertEquals` картой, не по одному
  полю), заголовки части `file` подстрокой, объявленная длина == числу записанных байт,
  401 → `SttError(code, body)`, 2xx → тело как есть.
- `TranscriptMapperTest`: восемь реплик фикстуры точным текстом; порог 1500 мс с обеих сторон
  (свидетели 1320 мс и ровно 1500 мс литералом), разрез по паузе 5020 мс у **того же** спикера,
  разрез по смене спикера, `audio_event` точной подстрокой `отчёт [смеется] лежит` плюс два
  `assertFalse` на обёртки, номер спикера по цифре `speaker_id` (литерал со `speaker_1` первым).
  **Проверил ожидание критерия 2 сам**: заново собрал правило `bin/stt-compare-html`
  (`GAP = 1.5`, пауза от `end` предыдущего до `start` следующего, `spacing` мимо) и прогнал по
  фикстуре — вышли те же 8 реплик слово в слово, единственное расхождение `([смеется])` против
  `[смеется]` (решение Р7). То есть литерал в тесте — мерило стенда, а не запись вывода маппера.
- `TranscribeWorkerTest` (через `transcribeNote`): нет аудио → `failure`, готовый `transcript.md`
  → `success` без запроса и без перезаписи, нет ключа → `retry` и ноль вызовов, 401 → `failure`,
  429/500/503 → `retry`, `IOException` → `retry`, `transcript.json` побайтно равен ответу.
- `DetailScreenTest`: каталог старой заметки Deepgram открывается, транскрипт виден (Robolectric,
  `@GraphicsMode(NATIVE)` на классе — есть).
- Фикстура: **проверил приватность сам, не принял на слово**. 264 элемента, `start`/`end`/`type`/
  `speaker_id`/`logprob` совпали с боевым окном `≤ 56,2 с` побайтно (`True`); пословный поиск
  (`(?<!\w)…(?!\w)`, без регистра) 95 уникальных слов боевого окна по всему тексту фикстуры — **0
  совпадений**; со всем 66-минутным ответом пересечение 43 служебных слова (`но`, `для`, `при` …),
  что и объявлено в журнале. Личного текста в публичном репо нет.

**НЕ видят (долг, часть объявлена, часть нет).**

- **Боевой транспорт `ElevenLabsClient.httpPost` не покрыт ничем**: `requestMethod = "POST"`,
  оба таймаута (30 с / 300 000 мс), `setFixedLengthStreamingMode`, выбор `errorStream` при не-2xx,
  `disconnect()`. Тест ходит мимо него через шов. Это цена шва, у `GithubClient` та же.
- **Живого запроса к вендору не было ни одного** — объявлено (риск 1 журнала), держится
  прокликкой владельца.
- **`doWork` не покрыт** — см. находку Н1: страж не отбирает цели по имени файла, он их просто не
  имеет; дырка не «по построению», а по отсутствию.
- **Скорость маппера на 18 525 элементах не мерена** — объявлено «не домерено» (риск 2).
- `Settings`/`MainActivity` не упоминаются **ни в одном** тесте репо (`grep` по
  `app/src/test/kotlin`): переключение онбординга и `setupComplete()` на новый ключ держится
  только чтением глазами.

### НАХОДКИ

**Н1. `app/src/main/kotlin/com/roflochinsky/noteapp/pipeline/TranscribeWorker.kt:25-33`** — что:
`doWork` (какая настройка читается и какой клиент зовётся) не держится ни одним тестом; стражи
начинаются с `transcribeNote`, куда вендор уже приходит параметром. Почему нарушение: срез объявлен
классом «тихая и дорогая ошибка» (задание исполнителя, правило 3), а именно смена вендора и есть
предмет ADR `2026-09-06-stt-elevenlabs-scribe-v2`. Замена `Settings.elevenLabsKey` на
`deepgramKey` или `ElevenLabsClient()::transcribe` на `DeepgramClient::transcribe` пройдёт все 310
тестов и весь набор M1–M5 (их фильтры `tests` сужены до `TranscriptMapperTest` и
`TranscribeWorkerTest`) — и владелец получит либо вечный `retry` без видимой причины, либо заметку
от старого вендора, которую `TranscribeWorker.kt:26` закрепит навсегда. Довод вместо мутации
(мутации мне запрещены): `grep -rln "Settings\.\|setupComplete" app/src/test/kotlin` — пусто.
Предлагаемый класс: `t`.

**Н2. `app/src/test/kotlin/com/roflochinsky/noteapp/pipeline/TranscribeWorkerTest.kt:45-56`** — что:
счётчик запросов `calls` увеличивается дважды за один вызов, когда `stt` берётся по умолчанию:
инкремент стоит и в значении по умолчанию (строки 48-51), и в обёртке, через которую `run` зовёт
`transcribeNote` (строки 53-56). Плюс положительная сторона бюджета «ровно 1 запрос на запись» не
проверяется вообще: `calls` сверяется только с нулём (строки 113, 126, 133). Почему нарушение:
правило 4 задания «тест — мерило»: мерило, которое считает вдвое, врёт молча, а деньги среза —
именно число запросов; сегодня спасает только то, что все три сверки нулевые. Довод: чтение,
мутация не нужна — дубль виден в диффе. Предлагаемый класс: `t`.

**Н3. `app/src/main/kotlin/com/roflochinsky/noteapp/pipeline/ElevenLabsClient.kt:67-70`** — что:
KDoc «Замороженный контракт запроса (Решение 2 ADR) — ровно шесть полей вместе с `file`». Решение 2
ADR морозит семь позиций, `keyterms` в их числе (`docs/adr/2026-09-06-stt-elevenlabs-scribe-v2.md`,
раздел «Решение 2»). «Ровно шесть» — это подмножество среза, а не текст ADR. Почему нарушение:
конвенция репо «KDoc объясняет ПОЧЕМУ и не врёт» (`CLAUDE.md` → рубрика ревью; образцы
`GithubClient.kt`); следующий агент прочитает это как запрет ADR на седьмое поле и споткнётся о
часть 2. Соседняя строка про «словарь приезжает частью 2» противоречие снимает, но заголовок всё
равно приписывает ADR лишнее. Предлагаемый класс: `b`.

**Н4. `app/src/test/kotlin/com/roflochinsky/noteapp/ui/DetailScreenTest.kt:281`** — что:
`onAllNodesWithText("Спикер 1").assertCountEquals(md.lines().size)` сверяет число подписей одного
говорящего с числом строк транскрипта. Равенство держится только потому, что в фикстуре Deepgram
все реплики одного спикера (проверил: `deepgram-sample-response.json` — 4 реплики, `speaker` = 0 у
всех). Читается как «экран нарисовал все реплики», меряет — «все реплики принадлежат Спикеру 1».
Почему нарушение: правило 4 задания «тест — мерило, а не задание» и рубрика «проверять поведение,
а не совпадение» (`CLAUDE.md` → _Тесты экранов_); второй говорящий в фикстуре покрасит тест по
причине, не связанной с экраном. Ложного зелёного нет — счёт при потере строк упадёт. Довод:
чтение фикстуры (счёт спикеров выше). Предлагаемый класс: `e`.

**Н5. `docs/SETUP.md:10,71,89`** — что: трижды сказано, что у ключа должно быть право
«Speech to Text». Ни ADR, ни `docs/research/stt-choice.md`, ни стенд такого требования не
называют — источника у утверждения нет (цифра «4,5 часа Scribe v2» источник имеет: ADR, раздел
«Решение 2»/«Последствия», сверил). Почему нарушение: `docs/SETUP.md` — мануал владельца, а не
черновик; непроверенная инструкция «выдай ключу право» стоит владельцу лишнего похода в кабинет
или ложного диагноза при отказе. Довод: сверка с ADR и research. Предлагаемый класс: `b`.

**Н6. Долг, а не нарушение** — `Settings.kt:35` (`deepgramKey`) и `DeepgramClient.kt` целиком после
среза остались **без единого потребителя** (`grep` по `app/src`, кроме упоминаний в KDoc). Это
объявленный путь отката ADR, и правило 1 границ правки прямо запрещало их удалять — исполнитель
поступил верно и написал об этом (раздел 7 журнала). Записываю для ведущего: detekt мёртвый
`public`-код не ловит, так что протухание пути отката никто не заметит; судьба — за владельцем на
закрытии эпика v3. Класс не предлагаю.

**Чего в находках нет — проверил и вопросов не имею.** Шов транспорта сделан по `GithubClient`, а
не по `DeepgramClient` (решение Р4) ✓; тело стримится, `ByteArray` нет ✓; `SttError` отдельным
типом ✓; диспетчера «разбор по форме JSON» нет ✓; ветки «выбор клиента по ключу» нет ✓;
`fromDeepgramJson` и его тесты не тронуты ✓; `OnboardingScreen.kt` и `app/build.gradle.kts` не
тронуты ✓; спекулятивных абстракций, мёртвой гибкости и хелперов под одного потребителя не нашёл —
`resource()` в `TranscriptMapperTest` появился ровно под второго потребителя, `WorkResult` —
вынужденный алиас (в `companion object` голый `Result` резолвится в `kotlin.Result`), Робик в
`TranscribeWorkerTest` вынужден (`app/build.gradle.kts:54` — `returnDefaultValues` не включён,
`android.util.Log` в чистом JVM-тесте бросает); изменённые файлы совпадают со строкой «Трогает:»
12 к 12, коммит один, сообщение по-русски со ссылкой на bd-id ✓; ADR-гейт: ADR есть и подписан,
формат заметки не менялся ✓.

**Оговорка о полноте.** Прод-кода тронуто заведомо больше 20 строк (`ElevenLabsClient` +129,
`TranscribeWorker` +67/−19, `TranscriptMapper` +58, `Settings` +19, `MainActivity` +12/−8) — по
таблице `docs/harness/epic.md` уровень повторного ревью выбирается ведущим, и «П» здесь наступает
по объёму независимо от моих классов.

**APPROVE** — находок 5 плюс один объявленный долг; класса `a` нет, `t` (Н1, Н2) ни одну находку
класса `a` этого среза не прикрывают, поэтому порог `CHANGES REQUESTED` не достигнут.
