# Узкое подтверждение (уровень У) — срез nikitatrubaev-u4g.2, ось `<ОСЬ>`

Это **не полное ревью**. Уровень У по `docs/harness/epic.md`: круг фикса содержал стража класса
«тихая и дорогая», поэтому проверяется ровно одно — **закрыты ли названные находки**. Новых
находок не заводи: всё, что попадётся сверх списка, пиши одной строкой в раздел «Замечено сверх
задания», без класса и без блокировки.

**Важное про контекст.** Круги ревью проходили другие агенты (машина уходила в нехватку памяти, и
удержанные ревьюеры были освобождены). Их вердикты даны файлами — читай как чужие и проверяй.

## Дерево и что смотреть

`/home/nikitatrubaev/orca/workspaces/noteapp/nikitatrubaev-u4g.2`, ветка `nikitatrubaev-u4g.2`.

- Новый HEAD: **`150f29f`** (второй круг фикса).
- Дифф этого круга: `git -C <дерево> diff 52bb9b6..150f29f`.
- Дифф всего среза, если понадобится контекст: `git -C <дерево> diff main...HEAD`.

Файлы не правишь, `bin/gate` и `scripts/mutate.py` не запускаешь — прогнаны, вывод дан входом.

## Входы

Каталог `/home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/`:

- вердикты второго круга: `nikitatrubaev-u4g.2-review2-spec.md`, `nikitatrubaev-u4g.2-review2-std.md`
- задание круга фикса: `nikitatrubaev-u4g.2-fix2-prompt.md`
- журнал исполнителя, раздел «Второй круг фикса»: `nikitatrubaev-u4g.2.md`

Скратчпад с прогонами:
`/tmp/claude-1000/-home-nikitatrubaev-code-noteapp/f6448f97-85c9-429a-8a98-7b14a4fd648a/scratchpad/`
— `nikitatrubaev-u4g.2-gates-fix2.txt` (sha первой строкой),
`nikitatrubaev-u4g.2-mutations-fix2.txt`, `nikitatrubaev-u4g.2-mutations-fix2-table.md`.

## Что подтвердить — по каждому пункту «закрыта / не закрыта», с доводом

Довод — не журнал исполнителя, а сам тест: открой его и скажи, что он ВИДИТ.

1. **Блокер (ось СТАНДАРТЫ НН8).** `clearReason` в ветке нефатального `SttError` не исполнялся ни
   одним тестом и не бился ни одной мутацией; удаление строки проходило молча, и после «ошибка
   ElevenLabs 404» с последующим 429 экран продолжал показывать 404. Закрыто ли, и убивает ли
   мутация `M14` именно эту строку, а не соседнюю.
2. **Сужение отмены (ось СТАНДАРТЫ НН6, ось СПЕКА Н6).** Отмена цепочки теперь только у записей с
   файлом причины: идущая заливка 48 МБ не должна обрываться при сохранении неизменённого ключа.
   Проверь мутацию `M15` и тест: держит ли он ОБА случая — с причиной и без.
3. **Гейт.** Прими по sha (первая строка файла — `HEAD 150f29f…`). Скажи отдельно, настоящий ли
   прогон: `FROM-CACHE` в файле быть не должно ни одного, `testDebugUnitTest` — исполнен.
4. **Мутации.** Шестнадцать плюс управляющая, `--scope full`, rc=0, полный прогон под управляющей
   — ровно одно падение. Подтверди, что новые (`M14`, `M15`, `M16`) разделяющие, а не ломающие всё
   подряд.
5. **Твоя ось, только по спискам находок второго круга.** По каждой находке своей оси из
   `…-review2-<ось>.md` — закрыта, не закрыта или осознанно оставлена долгом. Ничего не
   перепроверяй сверх этого.

## Форма отчёта

Через шелл (`Write` в чужое дерево не пускает):

```
cat > /home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/nikitatrubaev-u4g.2-review3-<ось>.md <<'EOF'
...
EOF
```

`<ось>` — `spec` или `std`, та, что назначена задачей. Шапка: `## Узкое подтверждение (У) среза
nikitatrubaev-u4g.2 — ось <ТВОЯ>`, строка «ревьюер, модель, усилие, дерево, HEAD».

Разделы: пункты 1–5 выше; «Замечено сверх задания» (одной строкой на пункт, без классов); итог
одной строкой — `ПОДТВЕРЖДЕНО` либо `НЕ ПОДТВЕРЖДЕНО` с указанием, что именно осталось незакрытым.

Своих сабагентов не запускай. В чат — коротко: подтверждено или нет, что осталось.
