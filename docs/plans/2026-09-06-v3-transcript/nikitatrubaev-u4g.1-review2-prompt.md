# Второе ревью среза nikitatrubaev-u4g.1 (уровень П) — та же ось, что ты проходил

Ты продолжаешь свою работу: первый круг ты уже прошёл, вердикт написал. Прошёл **круг фикса** по
твоим находкам — проверь его и перепройди свою ось целиком на новом HEAD.

Уровень **П** (полное второе ревью) выбран ведущим по двум основаниям сразу: в круге были стражи
класса «тихая и дорогая» и прод-кода тронуто заведомо больше 20 строк.

## Что изменилось

- Новый HEAD ветки `nikitatrubaev-u4g.1`: **`e43d0c6`** (коммит «круг фикса v3 среза 1: закрыты
  дыры в стражах»), поверх прежнего `05ea1eb`. Дифф круга фикса — `git -C <дерево> diff
  05ea1eb..e43d0c6`; дифф всего среза — `git -C <дерево> diff main...HEAD`.
- Дерево прежнее: `/home/nikitatrubaev/orca/workspaces/noteapp/nikitatrubaev-u4g.1`.
- Задание круга фикса (что именно было велено закрыть):
  `/home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/nikitatrubaev-u4g.1-fix-prompt.md`
- Журнал исполнителя с разделом «Круг фикса»:
  `/home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/nikitatrubaev-u4g.1.md`
- Сводный вердикт ведущего по первому кругу (классы находок и что ведущий решил):
  `/home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/nikitatrubaev-u4g.1-verdict.md`
- Гейт круга фикса (sha первой строкой файла):
  `/tmp/claude-1000/-home-nikitatrubaev-code-noteapp/f6448f97-85c9-429a-8a98-7b14a4fd648a/scratchpad/nikitatrubaev-u4g.1-gates-fix.txt`
- Прогон мутаций круга фикса, восемь плюс управляющая, `--scope full`, rc=0:
  `…/scratchpad/nikitatrubaev-u4g.1-mutations-fix.txt`, спека — `…/nikitatrubaev-u4g.1-mutations-fix.toml`

## Правила те же

Файлы в дереве не правишь, `bin/gate` и `scripts/mutate.py` не запускаешь (вторая ось читает то же
дерево; гейт и мутации прогнаны). Гейты принимаешь по sha. Сообщаешь ВСЕ находки с предлагаемым
классом; класс утверждает ведущий.

## Что сделать

1. **По каждой своей находке первого круга — «закрыта / не закрыта», с доводом.** Не на слово
   исполнителя: посмотри тест, который её якобы закрывает, и скажи, что этот тест ВИДИТ.
2. **Перепройди свою ось целиком на новом HEAD** — круг фикса тронул и реализацию
   (`TranscribeWorker.transcribeById` вынесен из `doWork`, KDoc контракта в `ElevenLabsClient`), так
   что новые находки возможны. Пункты твоего вердикта из первого круга пройди заново.
3. **Отдельно посуди вынесенный шов.** Исполнитель вынес выбор каталога, ключа и вендора из
   `doWork` в `transcribeById` — по его словам, только затем, чтобы эти строки исполнял тест
   (`androidx.work:work-testing` в зависимостях нет, `app/build.gradle.kts` срез не трогает).
   Вопрос твоей оси: это шов под потребителя, который в срезе есть, или устройство ради теста?
4. **Проверь честность объявленного долга.** Исполнитель сам сказал, что вендор по умолчанию
   (`stt = ElevenLabsClient()::transcribe`) тестом по-прежнему не держится. Согласен ли ты, что
   это ровно то, чего не покрывает мутация `X1` (она подменяет ключ, а не вендора), и что долг
   назван полно.
5. Решение ведущего, которое обсуждать не надо (оно принято): онбординг и `setupComplete()`
   тестом в этом срезе не покрываются, долг перенесён в `u4g.2`. Если считаешь, что это неверно,
   скажи — но как находку, а не как блокер.

## Отчёт

Пиши через шелл в основное дерево (не `Write`):

```
cat > /home/nikitatrubaev/code/noteapp/docs/plans/2026-09-06-v3-transcript/nikitatrubaev-u4g.1-review2-<ось>.md <<'EOF'
...
EOF
```

где `<ось>` — `spec` или `std`, та же, что ты проходил. Шапка: `## Второе ревью (П) среза
nikitatrubaev-u4g.1 — ось <ТВОЯ>`, строка «ревьюер, модель, усилие, дерево, HEAD».

Разделы: «Мои находки первого круга — закрыта / не закрыта», твои пункты вердикта заново, «Новые
находки» (с классами) либо «новых находок нет», итог одной строкой `APPROVE` |
`CHANGES REQUESTED` (только за конкретное неверное поведение с примером вход → выход) |
`ВЛАДЕЛЬЦУ`.

Своих сабагентов не запускай. В чат — коротко: вердикт, сколько находок закрыто, сколько новых.
