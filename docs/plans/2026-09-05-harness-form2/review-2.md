# Ревью второго круга (уровень П) — фикс-цикл по review.md, коммиты ec2bba6 и 8a75ff4

Ревьюер: na-reviewer-роль в канале Agent, модель fable, усилие medium, тот же ревьюер в
продолжении контекста первого круга. Дерево — главное `/home/nikitatrubaev/code/noteapp`
(только чтение, своих правок нет). Диапазон `128e383..8a75ff4`, 14 файлов (+ beads .37/.38/cc9).

## 1. ГЕЙТЫ

HEAD `8a75ff494c91b6a0f28ab85a0cf64718193836ad`, `git status --short` пуст. Перегнал сам:

```
~/.local/bin/ruff check scripts            → All checks passed!            rc=0
~/.local/bin/ruff format --check scripts   → 11 files already formatted    rc=0
~/.local/bin/pytest scripts/tests -q       → 93 passed in 9.43s            rc=0   (было 91, +2 новых)
python3 .claude/hooks/tests/test_destructive_fs_guard.py → OK: 84/84 (block=53 allow=29 override=2)
python3 .claude/hooks/tests/test_orca_card_sync.py       → ok
python3 .claude/hooks/tests/test_reminders.py            → ok
python3 scripts/ready-slices.py nikitatrubaev-0rk        → rc=0, см. п. 2
```

Gradle и `bin/gate` не запускались по условию задания: Kotlin в диапазоне не менялся
(`git diff --stat` — ни одного `.kt`).

## 2. СПЕКА — по каждой находке первого круга

### Класс a — нарезка

| # | Статус | Доказательство |
|---|---|---|
| a-1 (.37 неизбежно трогает `MainActivity.kt`) | **закрыта** | `bd show .37` «Трогает:» теперь содержит `MainActivity.kt`, `DetailScreen.kt`; описание прямо говорит «точка монтирования диалога — MainActivity … onDelete → батч в RepoStore, удаление каталога записи в NotesStore, back()». По коду верно: `MainActivity.kt:194-209` — единственное место, где монтируется `DetailScreen`, у которого колбэка удаления нет (`DetailScreen.kt:84-95`). Ребро `.38 → .37` есть (`bd show .38`: DEPENDS ON .37; `bd show .37`: BLOCKS .38, .40). |
| a-2 (.38 неизбежно трогает `RepoStore.kt`) | **закрыта иначе** — не разведением файлов, а последовательностью | .38 описывает механику честно: «второй метод очереди рядом с pending(), оверлей view() продолжает её показывать; RepoStore, RepoWriteWorker и WriteQueue меняет .37, а MainActivity.store() — общая точка». По коду совпадает: `RepoStore.push()` (`RepoStore.kt:339-347`) берёт `queue.pending().firstOrNull()`, `view()` (`RepoStore.kt:107-118`) строит оверлей из того же `pending()`. Оба среза заявляют эти файлы, конфликта нет благодаря dep. |
| a-3 (.37 тронет `WriteQueue.kt`/`Edit.kt`) | **закрыта** | .37 «Трогает:» получил `WriteQueue.kt`, `Edit.kt`, `WriteQueueTest.kt`, `EditTest.kt`; описание: «метки группы в WriteQueue.Op и типа правки Edit „вырезать строку-ссылку“ — оба файла в этом срезе». `WriteQueue.Op` (`WriteQueue.kt:25`) и `Edit.DeleteFile` (`Edit.kt:39`) без изменений — предпосылка находки в силе, теперь она учтена. `.40` (тоже `Edit.kt`, `EditTest.kt`) зависит от .37 — ребро есть. |

Проверка «не остался ли незаявленный файл, который .37 неизбежно тронет»:

- «аудио нельзя удалить у неотправленной» — признак отправки это файл-маркер `NotesStore.PUSHED`
  (`PushWorker.kt:25`), читается через константу `NotesStore` — `PushWorker.kt` (.39) трогать не надо. ✓
- лента после удаления локального каталога — `MainActivity.feed()` = `NoteRef.merge(notes, repoNotes)`
  (`MainActivity.kt:285-286`), `notes` перечитывается `NotesStore.list` (`MainActivity.kt:128`) —
  `FeedScreen.kt` и `NoteRef.kt` не нужны. ✓
- «.34 findDonePath снять» — вызовы только в `GithubClientTest.kt:89`, `GithubApiTest.kt:27,33` (оба в
  списке .37). Упоминания в KDoc `DetailScreen.kt:76` (.37) и **`NoteRef.kt:23` (.39)** — комментарий,
  компиляцию не ломает, но станет ссылкой на несуществующую функцию (находка b-2 ниже).
- «.36 httpGet без ETag» — `GithubClient.kt:169` (в списке). ✓

Честно ли «параллельно с .37 может идти только .39»: файлы .39 (`RawNote.kt`, `PushWorker.kt`,
`NoteRef.kt`, `RawNoteTest.kt`, `NoteRefTest.kt`, `docs/adr/`) не пересекаются с 19 файлами .37,
замки не общие (у .39 — `adr`, у .37 — нет). .38 и .40 держатся рёбрами. Живой прогон:

```
=== МОЖНО ПУСТИТЬ СЕЙЧАС (1 из потолка 2) ===
  nikitatrubaev-0rk.37   Удаление доезжает с телефона … (19 файлов)
=== ЖДУТ (3) ===
  .39  заблокирован (bd ready не отдаёт: зависимость или status=blocked)
  .38  заблокирован (…)      .40  заблокирован (…)
```
`-n 3` даёт то же. Все файлы из «Трогает:» четырёх срезов существуют (`test -e` по каждому), кроме
`ui/Chip.kt` — заявлен как новый, так и должно быть. Утверждение плана верно.

### Класс t — mutate.py

| # | Статус | Доказательство |
|---|---|---|
| t-1 (красный тест без мутации = «убита») | **закрыта** для узкого набора | `mutate.py:288-302` `baseline()`: `run_gate(root, tests)` без подмены → `verdict()` → `Refusal` на `invalid` или `failed`. Вызов `mutate.py:384-385` — по каждому уникальному фильтру из `[*mutations, control]`, после `--only`, ДО создания журнала. Тест `test_red_baseline_refuses` (`test_mutate.py:423-441`) честен: добавляет в `tests.json` тест `CalcTest::already red` с недостижимым `must_contain`, коммитит (копия чистая), ждёт rc=2, «базовый прогон» и имя теста в stderr, отсутствие каталога журнала — всё это следствия настоящего кода, не подгонка. **Новый ложный путь есть, но в другую сторону** — находка t-1' ниже: для `--scope full` фильтр `""` не баселинится. |
| t-2 (`_strip_params` по первой `[`) | **закрыта** | `mutate.py:244-246` — `re.sub(r"\[[^\]]*\]$", "", name)`, только хвостовой блок. Тест `test_strip_params_only_trailing_block` (`test_mutate.py:444-453`): имя с `[a, b]` внутри не режется, `параметризованный[1]` → `параметризованный`, `matches` двух разных имён со скобками — `False`. Регресса к прежнему поведению нет: для `name[0]` результат тот же, что у `split("[")[0]`. Нет положительной проверки `matches("T::name[1]", "T::name") is True` — мелочь, b-3. |

### Классы b и e

| # | Статус | Где |
|---|---|---|
| b-1 режим 3, класс `a` без открытого среза | закрыта | `.claude/skills/epic/SKILL.md:149-151`: «новый срез этого же эпика (петля в режим 2) либо вопрос владельцу» |
| b-2 «один батч развилок — тоже пропуск» | закрыта | `epic/SKILL.md:29-30`, `discuss/SKILL.md:34-35` |
| b-3 когда гонять `--scope full` | закрыта | `epic/SKILL.md:117-119` (под управляющей на сданном HEAD и после слияния), `epic/SKILL.md:127-128` (шаг 7), `docs/harness/epic.md:148-149` |
| b-4 потолок `-n` | закрыта | `-n 2` везде: `epic/SKILL.md:57`, `epic-orca/SKILL.md:19,30`, `docs/harness/epic.md:103`, `ready-slices.py:10,108` (default 2), план `:1270`; grep `-n 3` — ноль совпадений |
| b-5 ruff.toml про backend/AGENTS.md | закрыта | `scripts/ruff.toml:1-2` |
| b-6 spec-check «R1 §7.2» без пометки | закрыта | `scripts/spec-check.py:1` — «из workwatch, форма 2 C2» |
| b-7 «SDK location not found» и `cp local.properties` | закрыта | `CLAUDE.md:24-25` |
| b-8 `ui/Chip.kt` не заявлен в .38 | закрыта | `bd show .38` «Трогает:» содержит `ui/Chip.kt`; в границах — «один приватный компонент в новом файле ui/Chip.kt» |
| b-9 `NotesStore.kt` не заявлен в .37 | закрыта | `bd show .37` «Трогает:» содержит `pipeline/NotesStore.kt` |
| e-1 «2 failed = выжила» не сказано человеку | закрыта иначе | `docs/harness/epic.md:149-152`, `epic/SKILL.md:117`. Формулировка «один класс ИЛИ один тест» оставляет вариант, у которого ровно та проблема, что описана следующей фразой — b-1 ниже |
| e-2 роутер «воркер» → epic-orca | закрыта | `.claude/hooks/skill-router.py:43` `\borca\b|epic-orca`; замер: «воркер записи падает на 304» → False, «эпик на Orca» → True, «orcaflow» → False, «на orca» → True |
| e-3 blocked-срез исчезал | закрыта | `ready-slices.py:127` `("open", "blocked")`, `:145-147`; .39 в живом выводе есть (см. выше). Причина одна на оба случая — b-4 ниже |
| e-4 `docs/research/v3-owner-checklist.md` вне «Трогает:» | закрыта | `bd show cc9` «Трогает:» содержит файл |
| e-5 почему без ponytail | закрыта | `.claude/agents/na-reviewer.md:17-19`; решение подтверждено вне репо — `~/.claude/CLAUDE.md:3` («Superpowers и ponytail 2026-09-05 (6 и 0 вызовов…)») |

Итого: 19 из 19 находок закрыты (a-2, e-1 — иначе, чем предлагалось, по существу закрыты).

## 3. МУТАЦИЯ

`scripts/mutate.py` гоняет Gradle-тесты приложения — Kotlin не менялся, поэтому не применимо в
этой форме. Вместо этого — три замера самих скриптов на подделках (скрипты в scratchpad
сессии, сами скрипты репо не тронуты):

1. **mutate.py, `--scope full` при красном тесте в классе вне узких фильтров** (поддельный
   `bin/gate` из `test_mutate.py` + `OtherTest::already red elsewhere`):
   ```
   --- scope=narrow rc=0     P1: убита · управляющая: убита
   --- scope=full   rc=0     P1: убита · управляющая: убита
        полный прогон под управляющей: выжила (2 failed) · широкий радиус: упало 2, заявлен один тест
   ```
   Красный тест прочитан как «широкий радиус», rc=0 — находка t-1'.
2. **ready-slices.py, срез `in_progress` делит файл с открытым** (поддельный `bd`: A in_progress
   и B open — оба `Трогает: docs/x.md`, `ready` отдаёт B):
   ```
   === МОЖНО ПУСТИТЬ СЕЙЧАС (1 из потолка 2) ===   e.2  B (open)   файлы: docs/x.md
   === ЖДУТ (0) ===
   ```
   B предложен, конфликт с уже взятым A невидим — находка t-2'.
3. Роутер — см. e-2 выше.

## 4. ФОРМАТ/ПРИВАТНОСТЬ

Срез формата заметки, аудио и ключей не касается: `.kt`, `docs/examples/process-notes.yml`,
`docs/adr/` в диффе нет. В описаниях bd тестовый репо назван явно (`voice-notes-test`), установка
на телефон — «владельцу». Ничего нового наружу не уходит.

## 5. СКОУП-КРИП

- ei5 «Трогает: .claude/, scripts/, docs/harness/, docs/agents/, CLAUDE.md, .gitignore, orca.yaml,
  .worktreeinclude» — `.claude/agents`, `.claude/hooks`, `.claude/skills`, `CLAUDE.md`,
  `docs/harness/epic.md`, `scripts/*` — в границах. ✓
- cc9 «Трогает: docs/plans/2026-08-26-tasks-v2.md, docs/research/v3-owner-checklist.md» —
  `docs/plans/2026-08-26-tasks-v2.md` в границах. ✓
- `docs/plans/2026-09-05-harness-form2/review.md` (237 строк, коммит ec2bba6) — вне обеих строк, но
  это журнал ревью; по `docs/harness/epic.md` журналы `docs/plans/<слаг>/…` — свои файлы и в
  «Трогает:» не пишутся. Не находка.

## 6. ГРАНИЦЫ

ВИЖУ: полный дифф 14 файлов; описания .37/.38/.39/.40/cc9/ei5 в bd и рёбра; код noteapp по путям
из «Трогает:» (`MainActivity`, `DetailScreen`, `RepoStore`, `WriteQueue`, `Edit`, `NotesStore`,
`PushWorker`, `GithubApi`, `GithubClient`, `NoteRef`); исполнение ruff/pytest/хуков;
`ready-slices` на живом bd и на подделке; `mutate.py` на подделке гейта в двух режимах; роутер на
пяти фразах. ДОЛГ: Gradle и `bin/gate` не гонялись (Kotlin не менялся); `mutate.py` на настоящем
Gradle не запускался; мутаций на сами Python-скрипты не ставил; `bd swarm`/`merge-slot` не звал.

## 7. НАХОДКИ

Классы предложены, утверждает ведущий. Ни одна не блокирует слияние по порогу
`docs/harness/epic.md` (нет `a`; `t` не прикрывают `a` этого среза).

- **t-1'. `scripts/mutate.py:384-385, 409-414` — базового прогона нет для полного набора, а его
  результат не входит в код выхода.** `baseline()` зовётся по фильтрам `m.tests`; `play(control,
  scope_full=True)` гоняет фильтр `""`. Уже красный тест в любом другом классе даёт «полный прогон
  под управляющей: выжила (2 failed) · широкий радиус», rc=0 (замер п. 3.1). Направление ошибки
  консервативное (не ложная «убита»), но новое правило `epic.md:148-149` делает `--scope full`
  обязательным после слияния — а его вердикт декоративен: `cmd_run` возвращает `SURVIVOR` только по
  `outcomes`, `full` влияет на rc лишь через `invalid`. Лечение в две строки: при `args.scope ==
  "full"` добавить `""` в набор `baseline` (один лишний прогон Gradle), и решить, входит ли
  `not full.killed` в rc (если да — код 1 и слово «полный» в причине).
- **t-2'. `scripts/ready-slices.py:127` — срезы `in_progress` не участвуют в расчёте занятых
  файлов.** Фильтр `("open", "blocked")` выбрасывает взятые срезы до `taken_files`; открытый срез,
  делящий с ними файл, выдаётся как «МОЖНО ПУСТИТЬ» (замер п. 3.2). Сценарий: круг из двух срезов,
  один закрылся, ведущий перезапускает скрипт, чтобы добрать слот, — второй ещё в работе и невидим.
  Унаследовано от workwatch (`== "open"` было и раньше), но строка правилась в этом коммите.
  Лечение: `in_progress` брать в `cand` с флагом «взят», класть его файлы и замки в `taken_*` до
  цикла и печатать в отдельной секции «В РАБОТЕ», не считая в потолок.
- **b-1. `docs/harness/epic.md:150-152`, `.claude/skills/epic/SKILL.md:117`:** «сужать до одного
  класса ИЛИ одного теста» — и следующей фразой «мутация, честно пойманная двумя тестами класса,
  читается „выжила“». Совет «один класс» имеет ровно ту проблему, которую объясняет. Точнее: «до
  одного теста (`*EditTest.имя*`); класс — только когда в нём один тест на эту ось».
- **b-2. `app/src/main/kotlin/com/roflochinsky/noteapp/pipeline/NoteRef.kt:23`:** KDoc «`findDonePath`
  … здесь и заканчивается» — после .37 (снятие `findDonePath` вместе с `GithubApi`, `.34`) ссылка
  повиснет, а файл принадлежит .39. Либо дописать в .37 «KDoc в NoteRef не трогать, поправит .39»,
  либо перенести правку комментария в .39 явной строкой описания — иначе исполнитель .37 выйдет за
  «Трогает:» ради одной строки.
- **b-3. `scripts/tests/test_mutate.py:444-453`:** у `_strip_params` нет положительного случая
  через `matches` (`matches("com.x.T::name[1]", "T::name") is True`) — регресс «перестали резать
  хвост» тест не поймает. Одна строка.
- **b-4. `scripts/ready-slices.py:145-147`:** причина «зависимость или status=blocked» одна на оба
  случая, хотя `i["status"]` под рукой; докстринг обещает «ЧЕМ он занят». Печатать
  `status=blocked` для заблокированных и «зависимость» для остальных — ветка на статусе.
- **e-1. `.claude/hooks/skill-router.py:43`:** правило изменено без теста; в
  `.claude/hooks/tests/` роутера нет вовсе. Замер сделан руками (п. 2, e-2) — пять фраз в
  `test_skill_router.py` закрепили бы и это правило, и будущие.
- **e-2. `docs/plans/2026-08-26-tasks-v2.md:1270`:** строка 108 знаков в абзаце (не в таблице);
  в файле уже есть длиннее (`:184`, `:831`), конвенции на ширину markdown у репо нет — только для
  сведения, не нарушение.

Проверено и находкой НЕ стало: `test_red_baseline_refuses` не подогнан (rc=2 и текст — из
`Refusal`, каталог журнала не создаётся, потому что `baseline` идёт до `mkdir`); `--only`
применяется до `baseline` (лишних прогонов нет); `\borca\b` — юникодная граница в `re`, «на orca»
и «эпик на Orca» ловятся после `.lower()`; 13 старых тикетов и таблица «Нарезка 2» согласованы с
bd (.38 «Ждёт .37», .40 «Ждёт .37», .39 blocked); ребро .38 → .37 стоит в обе стороны в `bd show`;
`epic.md:148` «пять правил» — пять маркеров; коммиты по-русски, со ссылкой на id.

## Итог

**APPROVE.** Все 19 находок первого круга закрыты (a-2 и e-1 — иначе, чем предлагалось, но по
существу). Новых находок класса `a` нет. Две новые `t` (t-1', t-2') с замерами — дыры в самих
стражах харнесса, не прикрывающие `a` этого среза; по порогу `docs/harness/epic.md` слияние не
блокируют, остаются в вердикте. Пять `b`/`e` — текст и тесты.
